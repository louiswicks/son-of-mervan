## Phase 17.2 — Budget Templates [DONE] 2026-04-09
- `routers/templates.py`: New router with `GET /budget-templates` (public) and `POST /budget-templates/{id}/apply?month=YYYY-MM` (auth required). Five static templates: Student, Single Professional, Family, Frugal Saver, High Earner. Apply endpoint is additive — existing expense rows are left untouched. Returns 400 if month has no planned salary set; 404 for unknown template id. Amounts calculated as `salary * pct_of_salary / 100`, rounded to 2 dp.
- `main.py`: Imported and registered `templates_router`.
- `tests/test_templates.py`: 13 tests covering: list returns 200 no-auth, 5+ templates, required fields on each template, unique ids, unauthenticated apply returns 401, unknown template 404, no-salary 400, invalid month 422, rows created with correct amounts, additive idempotency (second apply creates 0 rows), all 5 template ids apply successfully.
- 604 backend tests pass; 91.88% coverage; ruff clean.

## Phase 17.1 — Expense Search & Advanced Filtering [DONE] 2026-04-09
- `routers/insights.py`: Added `GET /insights/search` endpoint. Accepts `q` (case-insensitive name substring), `category` (exact, case-insensitive), `from` / `to` (month range, inclusive), `sort` (`date` desc default or `amount` desc), `page` + `page_size` (1-based, max 100). Requires auth. All filtering done in Python after bulk fetch (Fernet encryption prevents SQL predicates). Also added Phase 17 to PRD.md with 5 new tasks.
- `tests/test_expense_search.py`: 18 tests covering: unauthenticated 401, empty DB, no-filter returns all, keyword filter, case-insensitive keyword, category filter, case-insensitive category, from/to range filters, combined filters, sort by amount and date, pagination (page 1 + page 2), deleted expense exclusion, response item shape, cross-user isolation.
- 591 backend tests pass; 91.78% coverage; ruff clean.

## Phase 16.5 — API Changelog Endpoint [DONE] 2026-04-09
- `core/config.py`: Added `VERSION = "1.16.0"` constant and `CHANGELOG` static list (8 entries, newest-first, covering Phases 1–16). Both are module-level so they can be imported by tests directly.
- `main.py`: Imported `VERSION` and `CHANGELOG` from `core.config`. Updated `/health` to report `VERSION` instead of the old hardcoded `"1.0.0"`. Added public `GET /version` endpoint — no auth required — returns `{ version, changelog }`.
- `tests/test_version.py`: 7 tests covering: 200 with no auth, response shape, version matches constant, changelog contains current version entry, all entries have required fields, changelog is non-empty, endpoint matches the module-level constant.
- 573 backend tests pass; 91.60% coverage; ruff clean.

## Phase 16.1 — Email Verification Resend [DONE] 2026-04-09
- `routers/signup.py`: Added `POST /auth/resend-verification` endpoint — accepts `{ email }`, returns generic 200 regardless of whether email exists (prevents enumeration). Sends a new JWT verify token only when: user exists + not yet verified + `verification_sent_at` > 2 minutes ago. Rate-limited 3/min per IP via slowapi. Updates `User.verification_sent_at` on send. Dev link returned when SMTP not configured (same pattern as signup).
- `web/src/api/auth.js`: Added `resendVerification(email)` Axios wrapper for `POST /auth/resend-verification`.
- `web/src/components/LoginPage.jsx`: (1) On 403 "verify" login error, `showResend` state surfaces a "Resend verification email" button inline in the error banner that calls `doResend()` with the current `identifier` value pre-filled. (2) "Didn't receive your verification email?" text button at the bottom of the login form toggles the resend UI when no error message is shown (covers fresh-page use case). Both flows update resend state and show success/error feedback.
- `tests/test_auth.py`: Added `TestResendVerification` class — 5 tests: success path (dev link returned for eligible unverified user), generic 200 for unknown email, generic 200 for already-verified user, cooldown suppresses resend within 2 minutes, `verification_sent_at` timestamp updated on successful resend.
- Also updated PRD.md: Phase 13.4 checkboxes corrected to `[x]` (was already implemented), Phase 16 tasks added.
- 546 backend tests pass; 91.41% coverage; ruff clean.

## Phase 15.2 — Open Banking OAuth Flow [DONE] 2026-04-08
- `core/config.py`: Added `TRUELAYER_CLIENT_ID`, `TRUELAYER_CLIENT_SECRET`, `TRUELAYER_REDIRECT_URI`, `TRUELAYER_SANDBOX` (default True) optional settings
- `models.py`: Added `BankConnectResponse` (auth_url), `BankConnectionResponse` (id, provider, account_id, last_synced_at, created_at, is_sandbox), `BankConnectionListResponse` Pydantic schemas
- `routers/banking.py` (new): HMAC-signed CSRF state helpers; `GET /banking/connect` returns TrueLayer auth URL; `GET /banking/callback` validates state, exchanges code for tokens via httpx, fetches first account_id, upserts encrypted `BankConnection`, redirects to frontend `/banking?connected=true`; `POST /banking/refresh/{id}` refreshes stored tokens; `GET /banking/connections` lists active connections (excludes disconnected_at rows, never exposes raw tokens)
- `main.py`: Imported and registered `banking_router`
- `tests/test_banking.py` (new): 22 tests across 4 classes — TestConnect (4 tests: 503 when unconfigured, auth_url returned, scopes present, unauthenticated 401), TestCallback (7 tests: creates BankConnection, invalid state 400, missing params 422×2, httpx error 502, 503 unconfigured, upsert on second call), TestRefreshConnection (5 tests: refreshes tokens, 404 not found, 404 wrong user, 503 unconfigured, unauthenticated 401), TestListConnections (6 tests: empty list, returns active, excludes disconnected, no tokens in response, unauthenticated 401, isolates by user)
- 503 backend tests pass; 91% coverage; ruff clean

## Phase 13.5 — Active Session Manager [DONE] 2026-04-08
- `database.py`: Added `user_agent` (Text, nullable) and `last_used_at` (DateTime, nullable) columns to `RefreshToken` model
- `alembic/versions/t0u1v2w3x4y5_add_session_metadata_to_refresh_tokens.py`: Migration adding both columns to `refresh_tokens` table; down_revision = s9t0u1v2w3x4
- `main.py`: Updated `/login` endpoint to capture `request.headers.get("user-agent", "")[:512]` and pass `user_agent` + `last_used_at=datetime.utcnow()` when creating `RefreshToken`
- `routers/totp.py`: Same user_agent capture added to the TOTP `/auth/2fa/verify-login` endpoint that also creates refresh tokens
- `routers/signup.py`: (1) Added `verify_token as _verify_token` import from security + `Optional` from typing; (2) Updated `/auth/refresh` to set `rt.last_used_at = datetime.utcnow()` before issuing the new access token; (3) Added `SessionResponse` Pydantic model (id, created_at, last_used_at, user_agent, is_current); (4) Added `GET /auth/sessions` — returns active (non-revoked, non-expired) sessions for current user, marks is_current via cookie hash comparison; (5) Added `DELETE /auth/sessions/{id}` — revokes single session (ownership enforced, 404 if not found or wrong owner, 400 if already revoked); (6) Added `DELETE /auth/sessions` — revokes all sessions except current (identified via cookie)
- `web/src/api/sessions.js` (new): `getSessions`, `revokeSession(id)`, `revokeAllOtherSessions` Axios wrappers
- `web/src/hooks/useProfile.js`: Added `useSessions`, `useRevokeSession`, `useRevokeAllOtherSessions` React Query hooks with toast feedback
- `web/src/components/AccountSettings.jsx`: Added "Active Sessions" Section between Email Preferences and Data & Privacy — lists sessions with user_agent, sign-in date, last-active date, "Current" badge, per-row "Sign out" button, and "Sign out all other devices" bulk button (shown only when 2+ non-current sessions exist)
- `tests/test_sessions.py`: 10 tests across 3 classes — TestGetSessions (4 tests: returns active sessions, excludes revoked, excludes expired, unauthenticated 401), TestRevokeSession (4 tests: revoke own, cross-user 404, nonexistent 404, unauthenticated 401), TestRevokeAllOtherSessions (2 tests: revokes others, unauthenticated 401)
- 481 backend tests pass; ruff clean; Phase 13 complete

## Phase 13.1 — Frontend Route-Based Code Splitting [DONE] 2026-04-08
- `web/src/router.jsx`: Replaced all 24 eager `import` statements for route-level page components with `React.lazy()` calls; added `lazy` to the React import; split wrapping into two helpers: `withPageBoundary` (uses existing `AsyncBoundary` = ErrorBoundary + Suspense) for protected routes, and `withPublicSuspense` (ErrorBoundary + `<Suspense fallback={<PageSpinner />}`) for public routes
- `PageSpinner` inline component: minimal centered CSS spinner (no dependency) used as Suspense fallback on public routes while the auth context is not yet available; uses `@keyframes spin` injected via `<style>` tag to avoid Tailwind dependency
- `AsyncBoundary` (existing): already contains `ErrorBoundary + <Suspense fallback={<SkeletonCard />}>`; reused for all protected (authenticated) routes without modification
- Non-route infrastructure components (`AuthGuard`, `ErrorBoundary`, `AsyncBoundary`, `Skeleton`) remain eagerly loaded since they are needed before any lazy chunk resolves
- `npm run build`: produces 35 JS chunks (vs 1 monolithic bundle); main.js is 509 kB; heavy library chunk (Recharts + dependencies) is 240 kB per-chunk loaded only when needed
- 51 frontend tests pass; ruff/eslint clean; no backend changes

## Phase 12.5 — Milestone Email Notifications [DONE] 2026-04-08
- `database.py`: Added `MilestoneNotificationSent` model — tracks sent milestone emails per user with a UNIQUE constraint on (user_id, milestone_type) to guarantee at-most-once delivery; milestone_type examples: "streak_3", "streak_6", "streak_12", "savings_goal_{id}", "debt_free"
- `email_utils.py`: Added three new email functions: `send_streak_milestone_email(to_email, streak_months)`, `send_savings_goal_complete_email(to_email, goal_name, target_amount, currency)`, `send_debt_payoff_email(to_email)` — all no-op with INFO log when SENDGRID_API_KEY is unset
- `routers/milestones.py` (new): `check_milestones(session_factory)` APScheduler job (1st of month, 09:00 UTC) — iterates all active verified users and checks three milestone types: (1) under-budget streak reaching 3/6/12 months (streak computed same way as GET /insights/streaks), (2) savings goals where sum of contributions >= target_amount, (3) all non-deleted debts at zero balance; uses MilestoneNotificationSent for deduplication — records row before sending so a SendGrid failure doesn't cause duplicate on retry
- `main.py`: Registered `check_milestones` as 6th APScheduler job (cron: day=1, hour=9, minute=0); updated startup log message
- `alembic/versions/p6q7r8s9t0u1_add_milestone_notifications_sent.py`: Migration adding milestone_notifications_sent table with id/user_id/milestone_type/sent_at columns, FK to users, and unique constraint on (user_id, milestone_type)
- `tests/test_milestones.py`: 18 new tests across 4 classes — TestStreakMilestones (6 tests), TestSavingsGoalMilestones (4 tests), TestDebtFreeMilestone (5 tests), TestMilestoneEmailDevMode (3 tests); all patching via routers.milestones.* to correctly intercept direct imports
- 455 backend tests pass; 89.01% coverage; ruff clean

## Phase 12.4 — Full Account Data Export (JSON) [DONE] 2026-04-08
- `routers/export.py`: Added `GET /export/full-backup` endpoint — rate-limited to 1/hour (slowapi); collects all non-deleted user data across 7 entity types: profile, months+expenses (decrypted), recurring expenses, savings goals with contributions (current_amount summed from SavingsContribution rows), debts, custom categories, net worth snapshots; serialises to JSON via `json.dumps(indent=2)` with `default=str` for dates; streams with `Content-Disposition: attachment; filename="backup-YYYY-MM-DD.json"` header; added 6 new imports (RecurringExpense, SavingsGoal, SavingsContribution, Debt, UserCategory, NetWorthSnapshot)
- `web/src/api/export.js`: Added `exportFullBackup()` — calls `GET /export/full-backup` with `responseType: blob`, triggers download via `downloadBlob()` helper
- `web/src/components/AccountSettings.jsx`: Added `import { exportFullBackup }` from api/export; added `backupLoading` state; added `handleDownloadBackup()` handler with 429-aware error toast; added "Data & Privacy" Section before Danger Zone with descriptive copy and "Download backup" button (aria-labelled, disabled during loading)
- `tests/test_export.py`: Added `TestFullBackupExport` class — 5 tests: returns 200 with JSON containing all 7 keys, unauthenticated returns 401/403, Content-Disposition header present with backup- prefix and .json suffix, expense data included and decrypted, cross-user isolation enforced
- 437 backend tests pass; 89.02% coverage; ruff clean

## Phase 11.3 — Cashflow Forecasting [DONE] 2026-04-08
- `routers/forecast.py` (new): `GET /forecast?months=3&salary_override=<float>` — fetches user's most recent salary_planned by decrypting all MonthlyData rows (picks latest by YYYY-MM); loads all non-deleted, non-expired RecurringExpenses active as of today; scales each to monthly cost (daily×days-in-month, weekly×4, monthly×1, yearly÷12); projects N months forward accumulating a running_balance; marks deficit=true when projected_balance<0. Returns `{ months, monthly_income, projection: [{month, projected_income, projected_expenses, projected_balance, running_balance, deficit}] }`.
- `main.py`: Imported and registered `forecast_router`
- `web/src/api/forecast.js`: `getForecast(months, salaryOverride)` Axios wrapper
- `web/src/hooks/useForecast.js`: `useForecast(months, salaryOverride)` React Query hook (staleTime 2min)
- `web/src/components/ForecastPage.jsx`: Recharts AreaChart with running-balance area, colour-coded dots (red=deficit, blue=healthy), zero-line ReferenceLine, custom tooltip showing income/expenses/net/running/deficit badge; 4-card summary row (income, total expenses, projected saving, deficit count); monthly breakdown table with deficit row highlighting; salary override input with apply/clear; 3/6/12-month period selector; empty state.
- `web/src/router.jsx`: Added `/forecast` route → `ForecastPage`
- `web/src/components/AuthGuard.jsx`: Added `Waves` lucide icon; added `["/forecast", "Forecast", Waves]` to `NAV_TABS`
- `tests/test_forecast.py`: 16 tests — default 3 months, custom months param, out-of-range 422, projection keys, unauthenticated 401/403, zero income when no data, salary_override applied, latest month salary used, override beats stored, monthly recurring deducted, deficit flagged, no deficit, expired recurring excluded, yearly amortised (÷12), running balance accumulates, multiple recurring summed
- All 386 backend tests pass; ruff clean; eslint 0 errors

## Phase 11.2 — Bank Statement CSV Import [DONE] 2026-04-08
- `routers/import_csv.py` (new): `POST /import/csv` multipart endpoint — decodes CSV (UTF-8 with BOM / latin-1 fallback), auto-detects date/description/amount columns from a list of common bank header names, parses dates in 10 formats, converts negative amounts to positive via abs(), keyword-categorises each row against 8-category keyword map, checks duplicates against existing user expenses (name_lower + rounded_amount + month set lookup); returns `CSVPreviewResponse` with rows, total, duplicates_count, parse_errors. `POST /import/csv/confirm` takes confirmed rows, gets-or-creates MonthlyData, upserts expenses by name+category (updates actual_amount if matched, inserts new otherwise), writes AuditLog per row; returns `{ imported, skipped }`. Both endpoints rate-limited (20/min preview, 10/min confirm) and auth-protected.
- `models.py`: Added `CSVPreviewRow`, `CSVPreviewResponse`, `CSVConfirmRow`, `CSVConfirmRequest`, `CSVImportResult` Pydantic schemas
- `main.py`: Imported and registered `import_csv_router`
- `web/src/api/import.js`: `previewCSVImport(file, month)` (multipart) and `confirmCSVImport(rows)` Axios wrappers
- `web/src/hooks/useImport.js`: `useCSVPreview` and `useCSVConfirm` React Query mutation hooks
- `web/src/components/ImportPage.jsx`: 3-step flow (upload → preview → done); drag-and-drop or file browser; optional month override; review table with per-row category dropdown (populated from `useCategories`), include checkboxes, select-all, duplicate warning badges; summary stats bar; confirm/start-over buttons
- `web/src/router.jsx`: Added `/import` route → `ImportPage` with `withPageBoundary`
- `web/src/components/AuthGuard.jsx`: Added `Upload` lucide icon; added `["/import", "Import", Upload]` to `NAV_TABS`
- `tests/test_import.py`: 22 tests — unauthenticated 4xx, single-row parse, auto-categorisation (entertainment/food/other), month inferred from date, multi-row, blank lines, zero/negative amounts, empty file 422, missing columns 422, invalid month 422, duplicate detection, non-duplicate not flagged, cross-user isolation, confirm creates expense, confirm skips excluded, confirm updates existing actual, confirm creates MonthlyData when missing, empty rows, invalid month skipped
- 370 backend tests pass; 87.95% coverage; ruff clean

## Phase 11.1 — Custom Expense Categories [DONE] 2026-04-08
- `database.py`: Added `UserCategory` model (id, user_id, name, color, is_default, created_at); names stored unencrypted (labels aren't sensitive, and Fernet non-determinism would break the per-user UNIQUE constraint); `DEFAULT_CATEGORIES` constant defines the 8 built-in entries with hex colours
- `models.py`: Added `UserCategoryCreate`, `UserCategoryUpdate`, `UserCategoryResponse` Pydantic schemas; imported `Field` from pydantic
- `routers/categories.py`: New router with `GET/POST/PUT/DELETE /categories`; `_get_or_seed()` helper lazy-seeds 8 defaults on a user's first GET; POST returns 409 on duplicate name; DELETE returns 400 for is_default categories; all endpoints enforce ownership (404 on cross-user access)
- `routers/alerts.py`: Removed `VALID_CATEGORIES` hard-gate (categories are now user-defined); replaced with `len(category) > 50` → 422 check so the field still has a sensible max length
- `main.py`: Added `categories as categories_router` import and `app.include_router(categories_router.router)`
- `alembic/versions/m3n4o5p6q7r8_add_user_categories.py`: Migration creates `user_categories` table with PK/FK indexes and `uq_user_category_name` unique constraint
- `web/src/api/categories.js`: Axios wrappers for GET/POST/PUT/DELETE /categories
- `web/src/hooks/useCategories.js`: `useCategories` (5-min stale), `useCreateCategory`, `useUpdateCategory`, `useDeleteCategory` React Query hooks; all mutations invalidate the `categories` cache key
- `web/src/components/CategoriesPage.jsx`: Full management UI — colored circle pills, inline edit form with HTML `<input type="color">` picker, add new category form, delete with confirmation; default categories show "default" badge and cannot be deleted; count summary footer
- `web/src/router.jsx`: Added `/categories` route
- `web/src/components/AuthGuard.jsx`: Added "Categories" nav tab with `Tag` lucide icon
- `web/src/components/SonOfMervan.jsx`: Replaced hardcoded `CATEGORIES` array with `useCategories()` hook + `useMemo`; falls back to 8 defaults while loading
- `web/src/components/MonthlyTracker.jsx`: Replaced `BASE_CATEGORIES` constant with hook result; `buildRowsFromExpenses` accepts `categories` as 3rd param (default `FALLBACK_CATEGORIES`); hook result passed at call site
- `web/src/components/BudgetAlertsPage.jsx`: `AlertForm` now accepts `categories` prop (falls back to `FALLBACK_CATEGORIES`); main page fetches via `useCategories` and passes down to both `AlertForm` instances
- `tests/test_categories.py`: 19 new tests — seeding behaviour, list, create (duplicate 409), update (rename/recolour/conflict 409/404), delete (success/default 400/404/cross-user 404)
- `tests/test_alerts.py`: Updated `test_create_invalid_category` → now verifies any ≤50-char category is accepted (201); added `test_create_category_too_long_returns_422`
- 348 backend tests pass; 87.51% coverage; ruff clean on new files

## Phase 10.2 — Advanced Expense Search & Filtering [DONE] 2026-04-08
- `main.py`: Added `GET /expenses/search?q=&category=&from=YYYY-MM&to=YYYY-MM&page=1&per_page=20` endpoint; loads all user MonthlyData, decrypts months and filters by date range in Python (Fernet non-deterministic), then decrypts each expense and applies case-insensitive partial-match on name and exact category filter; sorts results most-recent-month first; paginates with `X-Total-Count` and `X-Page` response headers; validates `from`/`to` format with `^\d{4}-\d{2}$` regex, 422 on invalid; 422 on `per_page` > 100
- `web/src/api/expenses.js`: Added `searchExpenses({ q, category, from, to, page, per_page })` — wraps `GET /expenses/search`, extracts `x-total-count` header into response
- `web/src/hooks/useExpenses.js`: Added `useExpenseSearch({ q, category, from, to, page, perPage })` React Query hook; enabled only when at least one filter is active (query, from, or to); 30s stale time
- `web/src/components/MonthlyTracker.jsx`: Added cross-month search panel above the monthly filter bar — text input (debounced 300ms via `useEffect`/`setTimeout`), category dropdown, from/to month pickers, "Clear search" button; search results rendered as a responsive table with month, name, category, planned and actual columns; pagination controls when results span multiple pages; search panel collapses to zero-height when inactive; no changes to existing monthly tracker behaviour
- `tests/test_expenses.py`: Added `TestExpenseSearch` class — 13 new tests: unauthenticated 401, empty query returns empty results, name partial match, case-insensitive match, category filter, `from` date range, `to` date range, soft-deleted excluded, cross-user isolation, response includes `month` field, pagination headers correct, invalid `from` format 422, invalid `to` format 422
- 328 backend tests pass; 87.07% coverage; ruff clean

## Phase 10.1 — Spending Anomaly Detection [DONE] 2026-04-08
- `routers/insights.py`: Added `GET /insights/anomalies?month=YYYY-MM&lookback=3` endpoint; computes per-category mean and std dev over prior `lookback` months (range 2–12, default 3); z-score ≥ 2.0 → high, ≥ 1.5 → medium, ≥ 1.0 + >30% above mean → low; zero-std-dev fallback uses pct_change thresholds (>100% → high, >50% → medium); results sorted high→medium→low; returns `{ month, anomalies, lookback_months, categories_analysed }`
- `web/src/api/insights.js`: Added `getAnomalyDetection(month, lookback)` API client function
- `web/src/hooks/useInsights.js`: Added `useAnomalyDetection(month, lookback)` React Query hook (2-min stale time)
- `web/src/components/InsightsPage.jsx`: Added `AnomalyAlertsSection` component with colour-coded severity cards (red=high, amber=medium, yellow=low), lookback dropdown selector (2/3/6/12 months), z-score and pct-change detail; integrated into InsightsPage between HealthScoreCard and AIReviewSection; `CheckCircle` icon when no anomalies detected
- `tests/test_insights.py`: Added `TestAnomalyDetection` class — 12 new tests: unauthenticated 401, missing/invalid month 422, lookback out-of-range 422, no data returns empty list, no historical baseline returns empty list, detects high-severity anomaly, ignores normal spending, cross-user isolation, severity sort order, lookback parameter respected
- 315 backend tests pass; 86.79% line coverage; ruff clean

## Phase 8.4 — Tax Filing Integration [DONE] 2026-04-08
- `routers/export.py`: Added `GET /export/tax-summary?tax_year=YYYY` (JSON, rate-limited 1/min) and `GET /export/tax-pdf?tax_year=YYYY` (SA302-style PDF, rate-limited 1/min); `_HMRC_CATEGORY_MAP` maps 8 internal categories to HMRC Self Assessment headings with `potentially_deductible` flag; `_tax_year_months()` helper generates the 12 calendar months for a UK tax year (April–March); `_build_tax_summary()` aggregates salary_actual, total_actual, and per-category expense actuals across the tax year; PDF includes income/savings table, per-category breakdown with HMRC headings and deductibility flag, potentially-deductible total, and professional advice disclaimer
- `models.py`: Added `TaxCategoryBreakdown` (category, hmrc_category, total_actual, months_with_data, potentially_deductible) and `TaxSummaryResponse` Pydantic schemas
- `web/src/api/export.js`: Added `getTaxSummary(taxYear)` (JSON) and `exportTaxPDF(taxYear)` (blob download) functions
- `web/src/components/TaxExportPage.jsx` (new): Tax year selector (current + 4 prior years, auto-detects current UK tax year); 4 KPI cards (total income, total expenses, net savings, potentially deductible total); responsive category table with HMRC headings, deductibility badges, and totals row; mobile card-stack layout; loading skeletons; disclaimer banner; "Load Summary" + "Download PDF" buttons
- `web/src/router.jsx`: Added `/tax` route → `TaxExportPage` with `withPageBoundary`
- `web/src/components/AuthGuard.jsx`: Imported `Receipt` from lucide-react; added `["/tax", "Tax", Receipt]` to `NAV_TABS` (desktop + mobile)
- `tests/test_export.py`: Added `TestTaxSummary` class — 9 tests: empty year returns zeros, data aggregation with 2 months, cross-user isolation, months-outside-year excluded, year validation (too old/too future), unauthenticated 401, tax PDF returns 200/503, PDF unauthenticated, PDF invalid year
- `tests/conftest.py`: Extended `make_month()` to accept `salary_actual` and `total_actual` kwargs (backward compatible, default 0.0)
- 9 new backend tests; 284 total, 85.79% coverage; ruff clean; build 302.67 kB gzip (+2.19 kB)

## Phase 9.5 — Annual Financial Calendar [DONE] 2026-04-08
- `web/src/components/CalendarPage.jsx` (new): 12-month grid calendar with year prev/next navigation; recurring expenses shown in each applicable month, colour-coded by category (Housing=blue, Transportation=green, Food=orange, Utilities=yellow, Insurance=purple, Healthcare=pink, Entertainment=indigo, Other=gray); yearly-frequency expenses appear only in their anniversary month; savings goal deadlines shown with Target icon in emerald; current month highlighted with blue ring + "Now" badge; legend bar; summary footer with total counts; 12-card loading skeleton
- `web/src/tests/CalendarPage.test.jsx` (new): 9 tests — heading/month rendering, loading skeletons, empty months, monthly recurring in all 12 months, yearly recurring only in anniversary month, savings goal in correct month, year navigation, summary footer counts, goal-without-target-date not displayed
- `web/src/router.jsx`: added `/calendar` route → `CalendarPage` with `withPageBoundary`
- `web/src/components/AuthGuard.jsx`: imported `CalendarDays` from lucide-react; added `["/calendar", "Calendar", CalendarDays]` to `NAV_TABS`
- 9 new frontend tests all pass (45 pass, 3 pre-existing AuthGuard failures unchanged); build clean at 300.48 kB gzip (+10.66 kB)
- Files: web/src/components/CalendarPage.jsx (new), web/src/tests/CalendarPage.test.jsx (new), web/src/router.jsx, web/src/components/AuthGuard.jsx, PRD.md, progress.txt

## Phase 9.3 — Progressive Web App (PWA) [DONE] 2026-04-08
- `web/public/service-worker.js` (new): cache-first strategy for same-origin static assets; network-first for cross-origin (API) requests; SPA offline fallback — navigation requests always served `/index.html` from cache; stale cache cleanup on activate
- `web/src/serviceWorkerRegistration.js` (new): `register()` / `unregister()` helpers; registration only occurs in `NODE_ENV=production` to prevent dev cache pollution
- `web/src/index.js`: imports and calls `registerSW()` after `reportWebVitals()`
- `web/src/components/InstallBanner.jsx` (new): listens for `beforeinstallprompt`, shows dismissible bottom-anchored card with "Install" / "Not now" actions; persists dismissal in `localStorage`; positioned above mobile bottom tab bar (`bottom-20`); accessible (`role="banner"`, `aria-label`)
- `web/src/App.jsx`: renders `<InstallBanner />` inside `AuthProvider` so it appears on all authenticated and unauthenticated routes
- `web/public/manifest.json` unchanged — `display: "standalone"`, `theme_color: "#111827"` were already correct
- 7 new tests in `web/src/tests/InstallBanner.test.jsx`: no-render before prompt, shows on prompt, hidden when dismissed-flag present, "Not now" sets flag + hides, X button sets flag + hides, Install calls `prompt()`, Install hides after accepted outcome
- 32 frontend tests (7 new + 25 existing); 25 pass (pre-existing AuthGuard failures unrelated to PWA)
- Files: web/public/service-worker.js (new), web/src/serviceWorkerRegistration.js (new), web/src/components/InstallBanner.jsx (new), web/src/tests/InstallBanner.test.jsx (new), web/src/App.jsx, web/src/index.js, PRD.md, progress.txt

## Phase 8.3 — Investment Portfolio Tracking [DONE] 2026-04-08
- `Investment` model with Fernet-encrypted fields: name, units, purchase_price, notes; plaintext: ticker, asset_type, currency — ticker stays unencrypted for daily yfinance price sync
- `InvestmentPrice` model stores plaintext daily price snapshots (investment_id, price, fetched_at)
- Alembic migration `k1l2m3n4o5p6_add_investments.py` adds both tables with composite indexes
- `routers/investments.py` — full CRUD: list (with P&L), create (immediate price fetch if ticker provided), update, soft-delete; portfolio summary endpoint; manual `/sync-prices` endpoint
- APScheduler job `sync_all_investment_prices` runs daily at 16:30 UTC (after market close) via yfinance; added `yfinance>=0.2.40` to requirements.txt
- All yfinance calls wrapped in try/except — price sync failure never breaks the app
- `InvestmentsPage.jsx` — summary cards (holdings count, total cost, portfolio value, unrealised gain/loss with colour-coded band), responsive holdings table (name/ticker/type/units/buy price/current price/value/gain%), add/edit modal, delete confirmation, "Sync Prices" button with spinner
- `web/src/api/investments.js`, `web/src/hooks/useInvestments.js` — full React Query integration
- `["/investments", "Portfolio", LineChart]` added to `NAV_TABS` in `AuthGuard.jsx`; `/investments` route added to `router.jsx`
- 21 new tests in `tests/test_investments.py` covering: list empty/owned/soft-deleted, create with/without ticker, price fetch success/failure, invalid asset_type, zero units, update, delete, ownership enforcement, summary empty/no-prices/with-prices, sync prices, unauthenticated access
- 265 backend tests pass, 86.95% coverage (above 80% threshold); ruff clean
- Files: database.py, models.py, routers/investments.py (new), alembic/versions/k1l2m3n4o5p6_add_investments.py (new), main.py, requirements.txt, web/src/api/investments.js (new), web/src/hooks/useInvestments.js (new), web/src/components/InvestmentsPage.jsx (new), web/src/router.jsx, web/src/components/AuthGuard.jsx, tests/test_investments.py (new), PRD.md, progress.txt

## Phase 8.1 — AI/LLM-Powered Financial Advice [DONE] 2026-04-08
- `POST /insights/ai-review?month=YYYY-MM` streams Claude's analysis as Server-Sent Events (SSE) using `anthropic` SDK (claude-haiku-4-5-20251001)
- Sends only anonymised category-level data to Claude: income totals, spending totals per category, savings rate, over-budget flags — no raw expense names
- Rate-limited to 3 requests/user/day via Redis atomic INCR with expire (falls back to in-memory dict when Redis unavailable)
- Returns HTTP 429 with `X-RateLimit-Remaining: 0` and `Retry-After: 86400` headers when limit exceeded; graceful error SSE event when `ANTHROPIC_API_KEY` not configured
- `requestAIReview()` in `web/src/api/insights.js` uses native `fetch()` with `ReadableStream` to accumulate SSE chunks progressively
- `AIReviewSection` component added to `InsightsPage.jsx` between Health Score and Monthly Summary — streaming cursor, error card, daily-use counter display, disabled state at limit
- 5 new tests in `TestAIReview` class covering: unauthenticated 401, missing month 422, invalid month 422, no-API-key error SSE, rate-limit 429, rate-limit header, empty-month SSE
- 232 backend tests pass, 86.37% coverage; ruff clean
- Files: `requirements.txt`, `core/config.py`, `routers/insights.py`, `tests/test_insights.py`, `web/src/api/insights.js`, `web/src/components/InsightsPage.jsx`, `PRD.md`, `progress.txt`

## Phase 7.3 — What-If Scenario Planner [DONE] 2026-04-07
- Created `web/src/components/ScenarioPlannerPage.jsx`: new `/scenarios` page with per-category spending sliders (−50% to +50%, step 5%), 3-card summary bar (baseline savings / monthly adjustment / scenario savings), 24-month area chart (Baseline vs Scenario using Recharts AreaChart), and per-savings-goal completion time delta
- Data: fetches current-month planned amounts from `GET /insights/monthly-summary` (via `getMonthlySummary`) and savings goals from `GET /savings-goals`; all projection calculations are instant client-side with no additional API calls
- Sliders drive `spendingAdjustment` memoised value; `scenarioSavings = baselineSavings - spendingAdjustment`; goal months = ceil(remaining / monthlySavings); delta badge shows "Xmo sooner / Xmo later"
- Empty state shown when no current-month planned categories exist
- Added `Sliders` icon from lucide-react to nav; added `["/scenarios", "What-If", Sliders]` entry to NAV_TABS in `AuthGuard.jsx`
- Added `/scenarios` route to `router.jsx`
- 225 backend tests pass; 25 frontend tests pass; build clean at 289.82 kB gzip (+7.99 kB)
- Files changed: web/src/components/ScenarioPlannerPage.jsx (new), web/src/components/AuthGuard.jsx, web/src/router.jsx, PRD.md, progress.txt

## Phase 1.1 — Remove Debug/Migration HTTP Endpoints [DONE] 2026-04-07
- Debug HTTP routes (`/run-migration`, `/cleanup-old-columns`, `/debug/check-month/{month}`) were already removed from `main.py` in earlier phases; CLI equivalents exist in `scripts/migrate.py`
- Replaced the remaining `print()` call in `core/config.py` with `logging.basicConfig` + `logging.critical()` — no `print()` calls remain in application code
- `core/logging_config.py` and `scripts/migrate.py` already existed with correct implementations
- 206 tests pass; ruff clean (only pre-existing alembic import warnings unrelated to this change)
- Files changed: `core/config.py`, `PRD.md`

## Phase 6.1 — Budget Page UI Redesign [DONE] 2026-04-07
- Fixed column header misalignment in `SonOfMervan.jsx`: Category header was `col-span-4` but the select was `col-span-3`; corrected to `col-span-3` with an explicit `col-span-1` spacer for the delete button column so headers now align exactly with inputs on desktop
- Added explicit `sm:hidden` labels (Name, Amount, Category) inside each expense row — visible on mobile, hidden on desktop where the shared column header row provides context
- Mobile expense rows now render in a bordered card (`border border-gray-200 dark:border-gray-700 p-3`) for clear visual separation at 320px
- Added `dark:placeholder-gray-500` to all expense inputs and selects for consistent dark mode placeholder legibility (previously only the salary input had this)
- WCAG 44×44px touch targets (`min-h-[44px]`) on all interactive elements on mobile, removed on desktop (`sm:min-h-0`) to avoid inflating row height
- All 25 frontend tests pass (SonOfMervan: 6/6); build clean at 281.83 kB gzip (+122 B)
- Files changed: web/src/components/SonOfMervan.jsx, PRD.md

## Phase 4.4 — Spending Insights and Trends [DONE] 2026-04-06
- Created `routers/insights.py` with three endpoints: `GET /insights/monthly-summary?month=YYYY-MM` (month-over-month % change per category, biggest overspend, net income, ranked plain-English insight cards capped at 5), `GET /insights/trends?months=6` (per-category actual spending per month + 3-month rolling averages + overall income/spending trend), `GET /insights/heatmap?year=YYYY` (monthly spending totals with quartile-based intensity levels 0–4)
- All three endpoints: decrypt data in Python (Fernet non-deterministic encryption), user-scoped, `verify_token` auth dependency; `_find_month` helper avoids N+1 by fetching all user rows once and filtering in Python
- Registered `insights_router` in `main.py`
- Created `web/src/api/insights.js`: `getMonthlySummary`, `getSpendingTrends`, `getSpendingHeatmap` Axios wrappers
- Created `web/src/hooks/useInsights.js`: `useMonthlySummary`, `useSpendingTrends`, `useSpendingHeatmap` React Query hooks (staleTime 2–5 min)
- Created `web/src/components/InsightsPage.jsx`: 4-section page — (1) Monthly Summary: month picker, KPI row (income/spent/net), plain-English insight cards (green/red with icons), category breakdown table with planned/actual/prev-month/% change; (2) Income vs Spending Trend: LineChart (income=green, spending=red); (3) Category Trends: multi-line LineChart per category with fixed colour palette; (4) Annual Heatmap: 12-square grid with blue intensity levels and quartile legend; all charts theme-aware; WCAG touch targets
- Updated `web/src/router.jsx`: added `/insights` route → `InsightsPage` with `withPageBoundary`
- Updated `web/src/components/AuthGuard.jsx`: imported `Lightbulb` from lucide-react; added `["/insights", "Insights", Lightbulb]` to `NAV_TABS` (desktop + mobile bottom bar)
- Tests: 94 backend tests pass (unchanged); frontend build clean, 0 errors, 1 pre-existing warning; bundle 241.84 kB gzip (+3.1 kB)
- Files changed: routers/insights.py (new), main.py, web/src/api/insights.js (new), web/src/hooks/useInsights.js (new), web/src/components/InsightsPage.jsx (new), web/src/router.jsx, web/src/components/AuthGuard.jsx, PRD.md

## Phase 4.3 — Budget Alerts and In-App Notifications [DONE] 2026-04-06
- Added `BudgetAlert` ORM model to `database.py`: `id`, `user_id`, `threshold_pct` (int), `active` (bool), `deleted_at`, `created_at`; encrypted field `category_encrypted` with hybrid property for Fernet en/decrypt
- Added `Notification` ORM model to `database.py`: `id`, `user_id`, `type` (unencrypted string, e.g. "budget_alert"), `read_at` (nullable — NULL = unread), `dedup_key` (unencrypted string e.g. "ba:{alert_id}:{YYYY-MM}" for deduplication), `created_at`; encrypted fields `title_encrypted`, `message_encrypted`
- Added Pydantic schemas to `models.py`: `BudgetAlertCreate`, `BudgetAlertUpdate`, `BudgetAlertResponse`, `NotificationResponse`, `NotificationListResponse`; also added `VALID_CATEGORIES` set
- Created Alembic migration `c4d5e6f7a8b9_add_budget_alerts.py`: creates `budget_alerts` (with indexes on id, user_id, deleted_at) and `notifications` (with indexes on id, user_id, dedup_key) tables; down_revision `b3c4d5e6f7a8`
- Created `routers/alerts.py`: `GET /budget-alerts` (list), `POST /budget-alerts` (create, 201, validates category + threshold range + no duplicate active alert per category), `PUT /budget-alerts/{id}` (update), `DELETE /budget-alerts/{id}` (soft-delete, 204); `GET /notifications` (list newest 50 + unread_count), `PATCH /notifications/{id}/read` (mark single read), `PATCH /notifications/read-all` (bulk mark read, 204), `DELETE /notifications/{id}` (hard-delete, 204); `check_budget_alerts(session_factory)` background function — iterates all active alerts, aggregates category planned/actual from current month's MonthlyExpense rows (decrypted in Python), fires notification + email when actual/planned >= threshold with dedup_key deduplication
- Updated `main.py`: imports `BudgetAlert`, `Notification` from `database`; imports and registers `alerts_router`; adds `check_budget_alerts` to APScheduler as a daily cron at 00:10 UTC (after recurring generation at 00:05)
- Added `send_budget_alert_email()` to `email_utils.py`: SendGrid email with category, percentage, actual/planned amounts and month; falls back to console log in dev
- Created `web/src/api/alerts.js`: `listAlerts`, `createAlert`, `updateAlert`, `deleteAlert`, `listNotifications`, `markNotificationRead`, `markAllRead`, `deleteNotification` Axios wrappers
- Created `web/src/hooks/useAlerts.js`: `useBudgetAlerts`, `useCreateAlert`, `useUpdateAlert`, `useDeleteAlert` for alerts; `useNotifications` (polls every 60s), `useMarkRead`, `useMarkAllRead`, `useDeleteNotification` for notifications — all with React Query cache invalidation and toast feedback
- Created `web/src/components/BudgetAlertsPage.jsx`: alert management page with create/edit/delete/toggle-active flows, threshold preset buttons (50/75/80/90/100%), empty state, info card explaining daily check schedule; accessible WCAG 44px touch targets
- Updated `web/src/components/AuthGuard.jsx`: added `Bell`, `BellOff`, `Trash2`, `X` from lucide-react; `BellButton` component shows red badge with unread count; `NotificationPanel` slide-over (fixed right, full height on mobile) with outside-click-to-close, read/delete per notification, mark-all-read, "Configure alert thresholds" footer button; notification bell in both desktop and mobile header; added "Alerts" to `NAV_TABS` (7 tabs total)
- Updated `web/src/router.jsx`: added `/alerts` route → `BudgetAlertsPage` with `withPageBoundary`
- Tests: 94 backend tests pass (unchanged); frontend build clean, 0 errors; bundle 238.74 kB gzip (+2.83 kB)
- Files changed: database.py, models.py, routers/alerts.py (new), alembic/versions/c4d5e6f7a8b9_add_budget_alerts.py (new), main.py, email_utils.py, web/src/api/alerts.js (new), web/src/hooks/useAlerts.js (new), web/src/components/BudgetAlertsPage.jsx (new), web/src/components/AuthGuard.jsx, web/src/router.jsx, PRD.md

## Phase 4.1 — Recurring Expenses [DONE] 2026-04-06
- Added `RecurringExpense` ORM model to `database.py`: `id`, `user_id`, `frequency` (daily/weekly/monthly/yearly string), `start_date`, `end_date`, `last_generated_at`, `deleted_at`, `created_at`; encrypted fields `name_encrypted`, `category_encrypted`, `planned_amount_encrypted` with hybrid properties for transparent Fernet en/decrypt
- Created Alembic migration `a7b8c9d0e1f2_add_recurring_expenses.py`: creates `recurring_expenses` table with indexes on `id`, `user_id`, `deleted_at`; down_revision `f6a7b8c9d0e1`
- Added `RecurringExpenseCreate`, `RecurringExpenseUpdate`, `RecurringExpenseResponse` Pydantic schemas + `VALID_FREQUENCIES` set to `models.py`
- Created `routers/recurring.py`: `GET /recurring-expenses` (list), `POST /recurring-expenses` (create, 201), `PUT /recurring-expenses/{id}` (update), `DELETE /recurring-expenses/{id}` (soft-delete, 204), `POST /recurring-expenses/generate` (manual trigger); generation logic in `_generate_for_user` and `generate_all_recurring` — scales amount by frequency (daily × days-in-month, weekly × 4), skips if past end_date or already generated this month, avoids duplicate rows via name+category match
- Updated `main.py`: imports `BackgroundScheduler` from APScheduler; registers `generate_all_recurring` as a daily cron job at 00:05 UTC on app startup via `@app.on_event("startup")`; shuts down scheduler on `@app.on_event("shutdown")`; registers `recurring_router`
- Added `apscheduler>=3.10.0,<4.0.0` to `requirements.txt`
- Created `web/src/api/recurring.js`: `listRecurring`, `createRecurring`, `updateRecurring`, `deleteRecurring`, `triggerGenerate` Axios wrappers
- Created `web/src/hooks/useRecurring.js`: `useRecurring` query + `useCreateRecurring`, `useUpdateRecurring`, `useDeleteRecurring`, `useTriggerGenerate` mutations with React Query invalidation and toast feedback
- Created `web/src/components/RecurringExpensesPage.jsx`: management page with add/edit/delete flows, frequency badge, "Generate now" button (calls `/recurring-expenses/generate`), empty state, ConfirmModal for deletion, mobile WCAG-compliant touch targets
- Updated `web/src/router.jsx`: added `/recurring` route wrapping `RecurringExpensesPage` with `withPageBoundary`
- Updated `web/src/components/AuthGuard.jsx`: added `Repeat` icon from lucide-react; added `["/recurring", "Recurring", Repeat]` to `NAV_TABS` (desktop + mobile bottom bar)
- Tests: 94 backend tests pass (87% coverage); 25 frontend tests pass; build 229.71 kB gzip (+2.08 kB)
- Files changed: database.py, alembic/versions/a7b8c9d0e1f2_add_recurring_expenses.py (new), models.py, routers/recurring.py (new), main.py, requirements.txt, web/src/api/recurring.js (new), web/src/hooks/useRecurring.js (new), web/src/components/RecurringExpensesPage.jsx (new), web/src/router.jsx, web/src/components/AuthGuard.jsx, PRD.md

## Phase 3.8 — Frontend Test Suite [DONE] 2026-04-06
- Updated `web/src/setupTests.js`: added `window.matchMedia` mock (required by `useTheme` hook reading `prefers-color-scheme`) and `ResizeObserver` mock (required by recharts)
- Updated `web/src/App.test.js`: fixed broken "learn react" smoke test; mocks `./api/client` to block ESM-only axios; mocks `AuthContext` to render login page in unauthenticated state
- Created `web/src/mocks/handlers.js`: MSW v1-style handler documentation for POST /login, POST /auth/signup, POST /calculate-budget, GET /monthly-tracker/:month, PUT /expenses/:id, DELETE /expenses/:id
- Created `web/src/tests/LoginPage.test.jsx`: 5 tests — renders inputs/links, successful login (calls handleLogin + navigates to /budget), error message display, disabled/loading button state
- Created `web/src/tests/AuthGuard.test.jsx`: 5 tests — loading spinner, unauthenticated redirect to /login, authenticated render with nav+outlet, logout flow, active nav tab aria-current
- Created `web/src/tests/SonOfMervan.test.jsx`: 6 tests — heading render, salary/expense inputs, empty state (no result cards), add expense row, calculate button payload, result cards after mutation resolves
- Created `web/src/tests/MonthlyTracker.test.jsx`: 8 tests — skeleton on isLoading, row rendering from trackerData, edit button only for rows with id, edit/cancel/save flows (save calls updateMutation.mutateAsync), delete button opens ConfirmModal, confirm delete calls deleteMutation.mutateAsync
- Mocked `recharts` in SonOfMervan + MonthlyTracker tests to avoid ResizeObserver/DOM issues
- Result: 25 tests passing across 5 test suites; all 4 PRD-specified component areas covered
- Files changed: web/src/setupTests.js, web/src/App.test.js, web/src/mocks/handlers.js (new), web/src/tests/LoginPage.test.jsx (new), web/src/tests/AuthGuard.test.jsx (new), web/src/tests/SonOfMervan.test.jsx (new), web/src/tests/MonthlyTracker.test.jsx (new), PRD.md

## Phase 3.7 — Mobile-First Responsive Design [DONE] 2026-04-06
- Created `web/src/styles/breakpoints.css`: breakpoint reference comments + `.safe-area-bottom` utility for iOS home indicator padding on bottom tab bar
- Updated `web/src/App.jsx`: imports `breakpoints.css`; changed toast position `bottom-right` → `top-right` to avoid collision with bottom tab bar
- Updated `web/src/components/AuthGuard.jsx`: removed mobile select dropdown; replaced with fixed bottom tab bar (`sm:hidden`, `position: fixed; bottom: 0; z-50`) using LayoutDashboard/Calendar/TrendingUp/Settings icons from lucide-react; tabs are 56px min-height (WCAG 2.5.5); mobile header now shows only theme toggle + logout (both `min-h-[44px]`); main content has `pb-24 sm:py-6` to clear the tab bar; desktop nav unchanged
- Updated `web/src/components/MonthlyTracker.jsx`: added mobile card stack layout (`block sm:hidden`) — each expense row renders as a card with category, optional name input, projected/actual grid, and diff/status row; all card interactive targets `min-w-[44px] min-h-[44px]`; mobile totals summary card at bottom; existing table layout wrapped in `hidden sm:block`; salary/filter/pagination inputs all have `min-h-[44px]`; dark mode classes added throughout
- Updated `web/src/components/SonOfMervan.jsx`: removed invalid `xs:grid-cols-2` (no xs breakpoint in Tailwind) → `sm:grid-cols-12`; updated col-span prefixes from `md:` to `sm:`; sticky calculate button bottom changed `bottom-3` → `bottom-20 sm:bottom-3` to clear bottom tab bar on mobile; expense input fields have `min-h-[44px]` and dark mode classes
- Build passes: 0 errors, 1 pre-existing warning; bundle 227.64 kB gzip (+1.02 kB)
- Files changed: web/src/styles/breakpoints.css (new), web/src/App.jsx, web/src/components/AuthGuard.jsx, web/src/components/MonthlyTracker.jsx, web/src/components/SonOfMervan.jsx, PRD.md

## Phase 3.6 — Dark Mode [DONE] 2026-04-06
- Created `web/src/styles/tokens.css`: CSS custom properties for layout (`--color-bg`, `--color-surface`, `--color-text`, `--color-border`, etc.) and Recharts chart colors (`--color-chart-primary/success/danger/grid/axis/tooltip-*`); light defaults on `:root`, dark overrides under `[data-theme="dark"]`
- Created `web/src/hooks/useTheme.js`: `useTheme()` hook reads `theme` from `useUiStore`; `useEffect` applies `data-theme` attribute + `dark` class to `document.documentElement` on every change; writes to `localStorage`; returns `{ theme, toggleTheme }`
- Updated `web/src/store/uiStore.js`: added `theme` state initialised from `localStorage` falling back to `window.matchMedia('(prefers-color-scheme: dark)')`, plus `setTheme` action
- Updated `web/public/index.html`: added `<script>tailwind.config = { darkMode: 'class' }</script>` after CDN so Tailwind `dark:` variants activate when `<html class="dark">` is set
- Updated `web/src/components/AuthGuard.jsx`: imported `Moon` and `Sun` from lucide-react and `useTheme`; added Moon/Sun icon toggle button to both desktop and mobile nav bars; added `dark:` Tailwind variants to header, nav buttons, select, and main layout wrapper (`bg-gray-100 dark:bg-gray-950`)
- Updated `web/src/App.jsx`: imports `./styles/tokens.css`
- Updated `web/src/components/AnnualOverview.jsx`: imports `useTheme`; derives `chartColors` object (primary, success, danger, grid, axis, tooltip) from `theme`; passes theme-aware stroke/fill/tick/contentStyle props to all Recharts components (two LinCharts); added `dark:` variants to card backgrounds, borders, table rows, skeleton, year select
- Updated `web/src/components/SonOfMervan.jsx`: imports `useTheme`; same `chartColors` pattern for LineChart (savings projection) and BarChart (expense breakdown); added `dark:` variants to page background, inputs card, chart card wrappers, and category breakdown list items
- Build passes: 0 errors, 1 pre-existing warning (unused React import in AuthContext); bundle 226.62 kB gzip (+1.18 kB)
- Files changed: web/src/styles/tokens.css (new), web/src/hooks/useTheme.js (new), web/src/store/uiStore.js, web/public/index.html, web/src/components/AuthGuard.jsx, web/src/App.jsx, web/src/components/AnnualOverview.jsx, web/src/components/SonOfMervan.jsx, PRD.md

## Phase 3.5 — Zustand Global State Management [DONE] 2026-04-06
- Installed `zustand@5` (^5.0.12)
- Created `web/src/store/authStore.js`: `useAuthStore` Zustand store with `{ isAuthenticated, token, loading }` + actions `handleLogin(token)`, `setToken(token)`, `clearAuth()`, `setLoading(loading)`; token held in-memory only, never persisted
- Created `web/src/store/uiStore.js`: `useUiStore` Zustand store with `{ activeModal }` + `openModal(name)` / `closeModal()` — ready for Phase 3.6 modal usage
- Updated `web/src/api/client.js`: removed module-level `_accessToken`, `setAccessToken`, `getAccessToken`, `_onUnauthorized`, `setOnUnauthorized`; request interceptor now reads `useAuthStore.getState().token`; 401 interceptor calls `useAuthStore.getState().setToken(newToken)` on refresh success and `.clearAuth()` on failure — no callback registration needed
- Updated `web/src/context/AuthContext.jsx`: removed all `useState` for auth; `AuthProvider` is a thin component that runs `refreshSession()` on mount and calls `handleLogin` / `setLoading` on the store; `useAuth()` reads from `useAuthStore` and adds `handleLogout` locally to avoid circular import (`authStore → api/auth → api/client → authStore`); all existing consumers of `useAuth()` unchanged
- `web/src/App.jsx` unchanged — `<AuthProvider>` still wraps the tree for the session-restore side effect
- Build passes: 0 errors, 0 warnings; bundle 225.44 kB gzip (+227 B for zustand)
- Files changed: web/src/store/authStore.js (new), web/src/store/uiStore.js (new), web/src/api/client.js, web/src/context/AuthContext.jsx, web/package.json, PRD.md

## Phase 3.4 — Global Error Boundaries [DONE] 2026-04-06
- Created `web/src/components/ErrorBoundary.jsx`: class component with `getDerivedStateFromError` + `componentDidCatch`; renders "Something went wrong" card UI with "Try again" (reset state) and "Reload page" buttons; accepts optional `fallback` render-prop for custom error UI
- Created `web/src/components/AsyncBoundary.jsx`: composes `ErrorBoundary` + `Suspense` (falls back to `SkeletonCard`) for async data sections
- Updated `web/src/App.jsx`: wrapped root tree in `<ErrorBoundary>` so any unhandled throw is caught at the top level
- Updated `web/src/router.jsx`: added `withPageBoundary()` helper that wraps each protected page element in its own `<ErrorBoundary>`; nav and other pages remain functional when one page throws
- Added error boundary CSS to `web/src/App.css`: `.error-boundary-root` / `.error-boundary-card` / `.error-boundary-btn` styles
- Build passes: 0 errors, 0 warnings; bundle 225.21 kB gzip (+325 B)
- Files changed: web/src/components/ErrorBoundary.jsx (new), web/src/components/AsyncBoundary.jsx (new), web/src/App.jsx, web/src/router.jsx, web/src/App.css, PRD.md

## Phase 3.3 — React Query + Loading States + Optimistic Updates [DONE] 2026-04-06
- Installed `@tanstack/react-query@5` (^5.96.2) and `react-hot-toast@2` (^2.6.0)
- Created `web/src/components/Skeleton.jsx`: `Skeleton` base component (animate-pulse) + `SkeletonCard`, `SkeletonTable`, `SkeletonChart` composites used during loading states
- Created `web/src/hooks/useAnnualSummary.js`: `useAnnualSummary(year)` — `useQuery` wrapping `getAnnualOverview`, `staleTime: 5 min`
- Created `web/src/hooks/useProfile.js`: `useProfile()` query + `useUpdateProfile`, `useChangePassword`, `useDeleteAccount` mutations — all with `toast.success/error` callbacks
- Created `web/src/hooks/useExpenses.js`: `useMonthlyTracker(month, opts)` query + `useSaveMonthlyTracker`, `useUpdateExpense`, `useDeleteExpense` mutations with query invalidation on success
- Created `web/src/hooks/useBudget.js`: `useCalculateBudget` mutation wrapping `calculateBudget`
- Updated `web/src/App.jsx`: wrapped app in `QueryClientProvider` + added `<Toaster position="bottom-right" />`; `QueryClient` configured with `retry: 1, refetchOnWindowFocus: false`
- Updated `AnnualOverview.jsx`: replaced `useState + useEffect` data loading with `useAnnualSummary` hook; shows `SkeletonCard × 3` + `SkeletonChart × 2` + inline row skeletons while loading
- Updated `AccountSettings.jsx`: replaced `useEffect` profile load with `useProfile` query; replaced manual loading flags with `mutation.isPending`; replaced `StatusBanner` components with `toast.success/error`
- Updated `SonOfMervan.jsx`: replaced `useState loading + alert()` with `useCalculateBudget` mutation; errors surfaced via toast
- Updated `MonthlyTracker.jsx`: replaced `useEffect` data fetch with `useMonthlyTracker` query + `useEffect` to sync query data into `rows` state; `SkeletonTable` shown on initial load; `saveEdit` and `confirmDelete` apply changes **optimistically** to `rows` before the API call completes, with rollback on error; removed all localStorage read/write logic (superseded by React Query 30s cache); all success/error feedback via `react-hot-toast` (replaced `Toast.jsx` usage)
- Build passes with 0 errors, 0 warnings; bundle 224.89 kB gzip (+17.5 kB for React Query + react-hot-toast)
- Files changed: web/src/components/Skeleton.jsx (new), web/src/hooks/useAnnualSummary.js (new), web/src/hooks/useProfile.js (new), web/src/hooks/useExpenses.js (new), web/src/hooks/useBudget.js (new), web/src/App.jsx, web/src/components/AnnualOverview.jsx, web/src/components/AccountSettings.jsx, web/src/components/SonOfMervan.jsx, web/src/components/MonthlyTracker.jsx, web/package.json, PRD.md

## Phase 3.2 — React Router v6 [DONE] 2026-04-06
- Installed `react-router-dom@6` (^6.30.3)
- Created `web/src/context/AuthContext.jsx`: `AuthProvider` component holds `isAuthenticated`, `token`, `loading` state; performs `refreshSession()` on startup; exposes `handleLogin` / `handleLogout`; registers `setOnUnauthorized` callback so Axios 401 interceptor can clear auth state; `useAuth()` hook for consumer access
- Created `web/src/components/AuthGuard.jsx`: layout component used as parent route; shows spinner while loading; `<Navigate to="/login">` when unauthenticated; renders `Nav` + `<Outlet />` when authenticated; `Nav` uses `useNavigate` and `useLocation` to drive tab buttons and mobile select — replaces the tab-switching logic in the old `App.js`
- Created `web/src/router.jsx`: `createHashRouter` with routes `/login`, `/register`, `/forgot-password`, `/verify-email`, `/reset-password` (public); `/`, `/budget`, `/expenses`, `/annual`, `/settings` (protected under `AuthGuard`); `*` catch-all redirects to `/`
- Created `web/src/App.jsx`: thin wrapper — `<AuthProvider>` wrapping `<RouterProvider router={router} />`; updated `web/src/index.js` to import `./App.jsx` explicitly
- Updated `LoginPage.jsx`: removed `onLogin`/`goToSignup`/`goToForgotPassword` props; uses `useAuth().handleLogin` + `useNavigate`; navigates to `/budget` after successful login
- Updated `SignUpPage.jsx`, `ForgotPasswordPage.jsx`: removed `goToLogin` prop; use `useNavigate("/login")`
- Updated `VerifyEmailPage.jsx`: removed `goToLogin` prop and `window.location.hash` parsing; uses `useSearchParams` to read token; uses `useNavigate`
- Updated `ResetPasswordPage.jsx`: removed `getTokenFromHash` helper and `goToLogin` prop; uses `useSearchParams` + `useNavigate`
- Updated `AccountSettings.jsx`: removed `token`/`onLogout` props; uses `useAuth().handleLogout` + `useNavigate("/login")` on deletion
- Updated `MonthlyTracker.jsx`: removed `token`/`onSaved` props; reads `token` from `useAuth()` for localStorage key derivation; removed `onSaved()` call
- Updated `SonOfMervan.jsx`: removed `token`/`onSaved` props; removed `onSaved()` call
- Updated `AnnualOverview.jsx`: removed `token`/`refreshKey` props; `useEffect` now depends only on `year` (component remounts on route navigation, ensuring fresh data)
- Build passes: `npm run build` compiles with 0 errors; bundle 207.39 kB gzip (+19.84 kB for react-router-dom)
- Files changed: web/src/context/AuthContext.jsx (new), web/src/components/AuthGuard.jsx (new), web/src/router.jsx (new), web/src/App.jsx (new), web/src/index.js, web/src/components/LoginPage.jsx, web/src/components/SignUpPage.jsx, web/src/components/ForgotPasswordPage.jsx, web/src/components/VerifyEmailPage.jsx, web/src/components/ResetPasswordPage.jsx, web/src/components/AccountSettings.jsx, web/src/components/MonthlyTracker.jsx, web/src/components/SonOfMervan.jsx, web/src/components/AnnualOverview.jsx, web/package.json, PRD.md

## Phase 3.1 — Centralised API Client [DONE] 2026-04-06
- Created `web/src/api/client.js`: single Axios instance reading base URL from `REACT_APP_API_URL` (falls back to Railway prod URL); `withCredentials: true` sends httpOnly cookie on every request; request interceptor injects in-memory access token via `setAccessToken()`; response interceptor catches 401, calls `/auth/refresh` (deduplicating concurrent retries), retries the original request with the new token, calls `setOnUnauthorized()` callback on refresh failure (skips retry for `/auth/refresh`, `/login`, `/auth/signup`)
- Created `web/src/api/auth.js`: `login`, `signup`, `verifyEmail`, `requestPasswordReset`, `confirmPasswordReset`, `refreshSession`, `logout` — all returning unwrapped data
- Created `web/src/api/budget.js`: `calculateBudget(payload, commit)`
- Created `web/src/api/expenses.js`: `getMonthlyTracker`, `saveMonthlyTracker`, `updateExpense`, `deleteExpense`, `getAnnualOverview`
- Created `web/src/api/users.js`: `getProfile`, `updateProfile`, `changePassword`, `deleteAccount`
- Created `web/.env.example` documenting `REACT_APP_API_URL`
- Updated `web/src/App.js`: removed hardcoded `API_BASE_URL`; calls `setAccessToken` and `setOnUnauthorized` on startup; uses `refreshSession` and `logout` from `api/auth.js`; removed all direct `fetch()` calls
- Updated all 9 components to remove local `API_BASE_URL` constants and direct HTTP calls: `LoginPage`, `SignUpPage`, `VerifyEmailPage`, `ForgotPasswordPage`, `ResetPasswordPage`, `SonOfMervan`, `MonthlyTracker`, `AnnualOverview`, `AccountSettings`
- Removed vestigial `localStorage.getItem('authToken')` fallbacks from `MonthlyTracker` (superseded by Phase 2.4)
- Build passes: `npm run build` compiles with 0 errors; bundle 187 kB gzip (-1.6 kB)
- Files changed: web/src/api/client.js (new), web/src/api/auth.js (new), web/src/api/budget.js (new), web/src/api/expenses.js (new), web/src/api/users.js (new), web/.env.example (new), web/src/App.js, web/src/components/LoginPage.jsx, web/src/components/SignUpPage.jsx, web/src/components/VerifyEmailPage.jsx, web/src/components/ForgotPasswordPage.jsx, web/src/components/ResetPasswordPage.jsx, web/src/components/SonOfMervan.jsx, web/src/components/MonthlyTracker.jsx, web/src/components/AnnualOverview.jsx, web/src/components/AccountSettings.jsx, PRD.md

## Phase 2.7 — Backend Test Suite [DONE] 2026-04-06
- Created `tests/conftest.py`: sets env vars before import, patches alembic migrations to skip at test startup, creates SQLite test DB with `Base.metadata.create_all`, autouse fixture truncates all tables + resets rate-limiter storage between tests; fixtures: `db`, `verified_user`, `second_user`, `client` (unauthenticated), `auth_client` (overrides `verify_token` + `get_db`); helper functions `make_month` and `make_expense`
- Created `tests/test_auth.py`: 48 tests covering root health, signup (success + 5 invalid cases), email verification, login (success + wrong password + unverified + soft-deleted + nonexistent), password reset flow (request always-200, confirm success/invalid/expired/used, invalidates old tokens), refresh token (valid/revoked/expired), logout, account management (get/update profile, change password, delete account)
- Created `tests/test_expenses.py`: 22 tests covering PUT /expenses/{id} (name/category/amounts/wrong-user/404/soft-deleted), DELETE /expenses/{id} (success/soft-delete-db/wrong-user/already-deleted/404), GET /monthly-tracker (empty/all expenses/soft-deleted exclusion/category filter/pagination/salary totals/id field), POST /monthly-tracker (new actuals/update existing/preserve planned/remaining calculation)
- Created `tests/test_budget.py`: 17 tests covering POST /calculate-budget (read-only/no-save/commit-saves-month/saves-expenses/upsert/overspend-rec/surplus-rec/savings-rate/category-breakdown/invalid-month/zero-salary), GET /overview/annual (empty/with-data/totals-aggregate/year-filter/no-param/user-isolation)
- Created `tests/test_encryption.py`: 13 tests covering encrypt/decrypt helpers (roundtrip/none/non-deterministic/missing-key), username field not plaintext, monthly data salary not plaintext, expense name/category/amounts not plaintext, bcrypt password hash
- Fixed pre-existing bug: added `@username.expression` to `User.username` hybrid property in `database.py` so class-level SQL filters don't raise `AttributeError` (Fernet-encrypted username can't be matched in SQL, but at least it doesn't crash)
- Added `pytest>=7.4.0`, `pytest-cov>=4.1.0`, `httpx>=0.24.0` to `requirements.txt`; created `pytest.ini`
- Result: 94 tests passing, 87% line coverage (target: 80%)
- Files changed: tests/conftest.py (new), tests/test_auth.py (new), tests/test_expenses.py (new), tests/test_budget.py (new), tests/test_encryption.py (new), pytest.ini (new), requirements.txt, database.py, PRD.md

## Phase 2.6 — Account Management [DONE] 2026-04-06
- Added `deleted_at` (nullable DateTime) column to `User` in database.py for 30-day soft-delete
- Created Alembic migration f6a7b8c9d0e1: adds `deleted_at` column + index on `users` table
- Created `routers/users.py` with four endpoints: `GET /users/me` (profile), `PUT /users/me` (update username), `PUT /users/me/password` (change password, requires current password), `DELETE /users/me` (soft-delete + revoke all refresh tokens)
- Registered `users_router` in main.py
- Updated `security.py` `verify_token` to reject soft-deleted users (HTTP 401)
- Updated `POST /login` in main.py to reject soft-deleted users (HTTP 403)
- Added `send_account_deletion_email()` to email_utils.py (SendGrid with console fallback in dev)
- `DELETE /users/me` revokes all active refresh tokens immediately, then sends deletion confirmation email in background
- Created `web/src/components/AccountSettings.jsx`: three sections — Profile (username edit, email read-only), Security (change password with policy validation), Danger Zone (type "DELETE" to confirm account deletion; auto-logout after deletion)
- Updated `web/src/App.js`: imported `AccountSettings`; added "Settings" tab to desktop nav + mobile select; renders `AccountSettings` with `token` and `onLogout` props
- Files changed: database.py, alembic/versions/f6a7b8c9d0e1_add_deleted_at_to_users.py (new), routers/users.py (new), main.py, security.py, email_utils.py, web/src/components/AccountSettings.jsx (new), web/src/App.js

## Phase 2.5 — Pagination and Filtering [DONE] 2026-04-06
- Added `PaginatedExpenseResponse` Pydantic schema to models.py: `{ items, total, page, pages, page_size }`
- Updated `GET /monthly-tracker/{month}` in main.py: added `category` (optional), `page` (default 1), `page_size` (default 25, max 100) query params; decrypts all expenses then filters/paginates in Python (required because fields are Fernet-encrypted); `expenses` field now returns paginated envelope instead of flat array; `rows` field preserved for backward compat
- Created Alembic migration e5f6a7b8c9d0: composite index `(monthly_data_id, deleted_at)` + standalone index `(deleted_at)` on `monthly_expenses` table
- Updated `web/src/components/MonthlyTracker.jsx`: added `filterCategory`, `currentPage`, `pagination` state; extracted `buildRowsFromExpenses()` helper; data fetch passes `page`, `page_size`, and optional `category` params; skips localStorage cache when filter is active; added filter bar with category dropdown and "Clear filter" button; added prev/next pagination controls (shown only when pages > 1); page resets to 1 on month or filter change
- Files changed: models.py, main.py, alembic/versions/e5f6a7b8c9d0_add_expense_indexes.py (new), web/src/components/MonthlyTracker.jsx

## Phase 2.4 — JWT Refresh Token Mechanism [DONE] 2026-04-06
- Added `RefreshToken` model to database.py: stores `token_hash` (SHA-256), `user_id`, `expires_at`, `revoked_at`, `created_at`; raw token never persisted
- Created Alembic migration d4e5f6a7b8c9 to add `refresh_tokens` table with indexes
- Added `REFRESH_TOKEN_TTL_DAYS: int = 30` to core/config.py
- Updated `POST /login` in main.py: access token TTL reduced 24h → 15 min; generates raw refresh token, stores SHA-256 hash, sets httpOnly cookie (`secure` in prod, `samesite=lax`, 30-day max-age)
- Added `POST /auth/refresh` to routers/signup.py: reads `refresh_token` cookie, validates hash + expiry + not revoked, issues new 15-min access token
- Added `POST /auth/logout` to routers/signup.py: revokes refresh token in DB, deletes cookie
- Updated `web/src/App.js`: removed localStorage token storage; on startup calls `POST /auth/refresh` with `credentials: "include"` to restore session from cookie; `handleLogin` stores token in React state only; `handleLogout` calls `/auth/logout` before clearing state
- 401 auto-retry Axios interceptor deferred to Phase 3.1 (centralized API client)
- Files changed: database.py, alembic/versions/d4e5f6a7b8c9_add_refresh_tokens.py (new), core/config.py, main.py, routers/signup.py, web/src/App.js

## Phase 2.3 — Password Reset Flow [DONE] 2026-04-06
- Added `PasswordResetToken` model to database.py: stores `token_hash` (SHA-256), `user_id`, `expires_at`, `used_at`; raw token never persisted
- Created Alembic migration c3d4e5f6a7b8 to add `password_reset_tokens` table with indexes
- Added `PASSWORD_RESET_TTL_MIN: int = 60` to core/config.py
- Added `send_password_reset_email()` to email_utils.py (SendGrid with console fallback in dev)
- Added `POST /auth/password-reset-request`: always returns success (prevents user enumeration), invalidates prior unused tokens, generates `secrets.token_urlsafe(32)` raw token, stores SHA-256 hash, sends email/dev link
- Added `POST /auth/password-reset-confirm`: hashes incoming token, validates against DB (404 if missing, 400 if used/expired), updates `password_hash`, marks token `used_at`
- Both endpoints enforce same password policy (uppercase, lowercase, digit, special char)
- Created `web/src/components/ForgotPasswordPage.jsx`: email form, shows dev link in non-prod
- Created `web/src/components/ResetPasswordPage.jsx`: reads token from hash query string, new+confirm password fields, success state shows "Go to login" button
- Updated `web/src/components/LoginPage.jsx`: added `goToForgotPassword` prop + "Forgot password?" link below form
- Updated `web/src/App.js`: added `#/reset-password` and `forgot-password` authMode routes; imports and renders new pages
- Files changed: database.py, alembic/versions/c3d4e5f6a7b8_add_password_reset_tokens.py (new), core/config.py, email_utils.py, routers/signup.py, web/src/components/ForgotPasswordPage.jsx (new), web/src/components/ResetPasswordPage.jsx (new), web/src/components/LoginPage.jsx, web/src/App.js

## Phase 2.2 — Full CRUD for Expenses and Income [DONE] 2026-04-06
- Added `deleted_at` (nullable DateTime) column to `MonthlyExpense` in database.py for soft-delete
- Created Alembic migration b2e4f5a6c7d8 to add `deleted_at`; upgrade/downgrade verified against SQLite
- Added `ExpenseUpdateRequest` and `ExpenseResponse` Pydantic schemas to models.py
- Added `PUT /expenses/{id}` endpoint: updates name/category/planned_amount/actual_amount, validates ownership (403 if wrong user)
- Added `DELETE /expenses/{id}` endpoint: soft-delete (sets `deleted_at`), returns 204, validates ownership
- Updated all existing expense queries in `calculate-budget`, `save_actuals`, and `get_monthly_tracker` to filter `deleted_at == None`
- Updated `GET /monthly-tracker/{month}` to return an `expenses` array with individual rows including DB `id` fields
- Created `web/src/components/ConfirmModal.jsx`: generic confirmation dialog with Cancel/Confirm buttons
- Updated `web/src/components/MonthlyTracker.jsx`:
  - Loads individual expenses from server `expenses` array; stores DB id per row
  - Pencil icon on saved rows triggers inline edit mode; Check/X icons to save/cancel
  - Inline save calls `PUT /expenses/{id}`; trash on saved rows opens ConfirmModal → `DELETE /expenses/{id}`
  - After batch save, re-fetches server data so new DB ids populate rows
- Files changed: database.py, alembic/versions/b2e4f5a6c7d8_add_deleted_at_to_monthly_expenses.py (new), models.py, main.py, web/src/components/ConfirmModal.jsx (new), web/src/components/MonthlyTracker.jsx

## Phase 2.1 — Alembic Database Migrations [DONE] 2026-04-06
- Added alembic>=1.13.0 and pinned sqlalchemy>=2.0.0 to requirements.txt
- Ran `alembic init alembic` to create scaffold (alembic/ dir + alembic.ini)
- Configured alembic/env.py: imports Base and engine from database.py, overrides sqlalchemy.url from DATABASE_URL env var, reuses app engine so PostgreSQL SSL args are inherited
- Generated initial migration (a047a3ff3bf1) via --autogenerate; captures users, monthly_data, monthly_expenses tables with all columns and indexes
- Replaced `init_db()` call in main.py with `alembic.command.upgrade(cfg, "head")` — schema is now migration-managed on every startup
- Verified: `alembic upgrade head` applies cleanly on a fresh DB; `alembic downgrade -1` rolls back; second startup is a no-op (already at head)
- Files changed: requirements.txt, alembic.ini (new), alembic/env.py (new), alembic/versions/a047a3ff3bf1_initial_schema.py (new), main.py

## Phase 1.4 — Security Headers & CORS Hardening [DONE] 2026-04-06
- Created middleware/security.py with SecurityHeadersMiddleware (Starlette BaseHTTPMiddleware)
- Headers added to every response: X-Content-Type-Options, X-Frame-Options, Content-Security-Policy, Referrer-Policy, Permissions-Policy
- HSTS (Strict-Transport-Security) added only when ENVIRONMENT=production (safe for local HTTP dev)
- Replaced wildcard CORS allow_methods=["*"] and allow_headers=["*"] with explicit safe lists
- Added ENVIRONMENT setting to core/config.py (default: "development")
- Documented ENVIRONMENT in .env.example
- Files changed: middleware/security.py (new), middleware/__init__.py (new), core/config.py, main.py, .env.example

## Phase 1.3 — Rate Limiting on Auth Endpoints [DONE] 2026-04-06
- Added slowapi==0.1.9 to requirements.txt
- Created core/limiter.py with shared Limiter instance (in-memory, keyed by remote IP)
- Registered limiter on app.state and RateLimitExceeded exception handler in main.py
- Applied @limiter.limit("5/minute") to POST /login (main.py) and POST /auth/signup (routers/signup.py)
- Both endpoints now accept request: Request as required by slowapi
- Returns HTTP 429 with Retry-After header on the 6th request within 60 seconds
- Files changed: core/limiter.py (new), main.py, routers/signup.py, requirements.txt

## Phase 1.2 — Fix JWT Secret Fallback [DONE] 2026-04-06
- Added core/config.py with Pydantic BaseSettings; JWT_SECRET_KEY is required (no default)
- App now exits immediately with a clear [FATAL] message if JWT_SECRET_KEY is missing
- security.py and main.py updated to import from core.config (no more os.getenv fallbacks)
- Added pydantic-settings>=2.0.0 to requirements.txt
- Created .env.example documenting all required and optional variables
- Files changed: core/config.py (new), security.py, main.py, requirements.txt, .env.example (new)

## Phase 1.1 — Remove debug/migration HTTP endpoints [DONE] 2026-04-06
- Removed POST /run-migration, POST /cleanup-old-columns, GET /debug/check-month/{month} from main.py
- Replaced all print() calls with stdlib logging (LOG_LEVEL env var controlled)
- Added core/logging_config.py and scripts/migrate.py CLI as replacement
- Files changed: main.py, email_utils.py, routers/signup.py, core/, scripts/

## Phase 4.2 — Savings Goals [DONE] 2026-04-06
- Added `SavingsGoal` ORM model to `database.py`: `id`, `user_id` (FK to users), `target_date` (nullable DateTime), `deleted_at`, `created_at`; encrypted fields `name_encrypted`, `target_amount_encrypted` with hybrid properties for transparent Fernet en/decrypt
- Added `SavingsContribution` ORM model to `database.py`: `id`, `goal_id` (FK to savings_goals), `contributed_at` (DateTime), `created_at`; encrypted fields `amount_encrypted`, `note_encrypted`; `contributions` relationship on SavingsGoal
- Added Pydantic schemas to `models.py`: `SavingsGoalCreate`, `SavingsGoalUpdate`, `SavingsGoalResponse` (includes computed `current_amount`, `status`, `required_monthly`), `SavingsContributionCreate`, `SavingsContributionResponse`
- Created Alembic migration `b3c4d5e6f7a8_add_savings_goals.py`: creates `savings_goals` and `savings_contributions` tables with indexes on id, user_id, deleted_at, goal_id; down_revision `a7b8c9d0e1f2`
- Created `routers/savings.py`: `GET /savings-goals` (list), `POST /savings-goals` (create, 201), `PUT /savings-goals/{id}` (update), `DELETE /savings-goals/{id}` (soft-delete, 204), `GET /savings-goals/{id}/contributions`, `POST /savings-goals/{id}/contributions` (add, 201), `DELETE /savings-goals/{id}/contributions/{contrib_id}` (hard-delete, 204); `_compute_status` derives on_track/behind/ahead/achieved/no_deadline from contribution pace vs required monthly rate
- Updated `main.py`: imports and registers `savings_router`
- Created `web/src/api/savings.js`: `listGoals`, `createGoal`, `updateGoal`, `deleteGoal`, `listContributions`, `addContribution`, `deleteContribution` Axios wrappers
- Created `web/src/hooks/useSavings.js`: `useSavingsGoals`, `useCreateGoal`, `useUpdateGoal`, `useDeleteGoal`, `useContributions`, `useAddContribution`, `useDeleteContribution` React Query hooks with toast feedback and cache invalidation
- Created `web/src/components/SavingsGoalsPage.jsx`: goal cards with `RadialBarChart` progress ring (Recharts), progress bar, status badge (on_track/behind/ahead/achieved/no_deadline), inline contribution panel (add/delete), create/edit forms; PiggyBank icon in nav
- Updated `web/src/router.jsx`: added `/savings` route → SavingsGoalsPage
- Updated `web/src/components/AuthGuard.jsx`: added Savings nav item (PiggyBank icon) for both desktop and mobile bottom tab bar
- Test result: 94 tests passing (unchanged); frontend build clean
- Files changed: database.py, models.py, routers/savings.py (new), alembic/versions/b3c4d5e6f7a8_add_savings_goals.py (new), main.py, web/src/api/savings.js (new), web/src/hooks/useSavings.js (new), web/src/components/SavingsGoalsPage.jsx (new), web/src/router.jsx, web/src/components/AuthGuard.jsx

## Phase 4.5 — Data Export (CSV and PDF) [DONE] 2026-04-06
- Created `routers/export.py` with two endpoints: `GET /export/csv?from=YYYY-MM&to=YYYY-MM` (streams all non-deleted expenses in the month range as a CSV with columns Month, Category, Name, Planned Amount, Actual Amount; returns empty CSV with headers when no data found); `GET /export/pdf?month=YYYY-MM` (generates a monthly budget report PDF using fpdf2 with salary summary table, per-category expense breakdown with overspend rows highlighted red, totals row, net savings row, and generated timestamp footer)
- Both endpoints: user-scoped via `verify_token`, rate-limited to 1 request/minute per IP using existing `slowapi` limiter, return 404 when month row not found for PDF, 422 on invalid month format
- Added `fpdf2>=2.7.0` to `requirements.txt`; installed locally
- Registered `export_router` in `main.py` (import + `app.include_router`)
- Created `web/src/api/export.js`: `exportCSV(fromMonth, toMonth)` and `exportPDF(month)` — both use `responseType: 'blob'` with the shared axios client, then trigger download via `URL.createObjectURL` + temporary `<a>` element
- Updated `web/src/components/MonthlyTracker.jsx`: added `ExportMenu` component (dropdown with "Download CSV" and "Download PDF Report" options, loading state, renders next to month picker) and imports (`Download` icon from lucide-react, `exportCSV`/`exportPDF` from api/export)
- Updated `web/src/components/AnnualOverview.jsx`: added `AnnualExportButton` component (single "CSV" button that exports the full selected year as `YYYY-01` to `YYYY-12`, loading state) and imports (`Download` icon, `exportCSV`)
- Tests: 94 backend tests pass unchanged; frontend build clean, 0 errors, 1 pre-existing warning; bundle 242.45 kB gzip (+612 B)

## Phase 4.6 — Audit Trail / Transaction History [DONE] 2026-04-06
- Added `AuditLog` ORM model to `database.py`: `id`, `user_id` (FK users), `expense_id` (no FK — persists after soft-delete), `action` (create/update/delete), `changed_fields` (JSON text, plaintext values), `timestamp`; `user` relationship
- Added `AuditLogResponse` Pydantic schema to `models.py`: `id`, `expense_id`, `action`, `changed_fields` (dict | None), `timestamp`
- Added `_expense_snapshot()` helper and `_write_audit()` helper in `main.py`; wired audit calls into `POST /monthly-tracker/{month}` (create, two code paths), `PUT /expenses/{id}` (update with before/after snapshot), `DELETE /expenses/{id}` (delete with before snapshot)
- Created `routers/audit.py`: `GET /audit/expenses/{expense_id}` — returns all audit entries for an expense newest-first; verifies caller owns the expense; returns 404 if no history, 403 if unauthorised
- Registered `audit_router` in `main.py`
- Created Alembic migration `g7h8i9j0k1l2_add_audit_logs.py`: creates `audit_logs` table with indexes on `id`, `user_id`, `expense_id`; `down_revision` = `c4d5e6f7a8b9`
- Created `web/src/api/audit.js`: `getExpenseAudit(expenseId)` Axios wrapper
- Created `web/src/hooks/useAudit.js`: `useExpenseAudit(expenseId, enabled)` React Query hook; enabled only when expenseId is non-null; 30s staleTime
- Updated `web/src/components/MonthlyTracker.jsx`: added `HistoryDrawer` component (slide-over panel, ACTION_LABEL/ACTION_COLOR maps, timeline list with before/after field diffs); clock icon button on each expense row (both desktop table and mobile card layouts); `historyExpense` state drives drawer open/close
- Tests: 94 backend tests pass unchanged; frontend build clean (243.42 kB gzip, 0 errors)
- Files changed: database.py, models.py, main.py, routers/audit.py (new), alembic/versions/g7h8i9j0k1l2_add_audit_logs.py (new), web/src/api/audit.js (new), web/src/hooks/useAudit.js (new), web/src/components/MonthlyTracker.jsx

## Phase 4.7 — Multi-Currency Support [DONE] 2026-04-06
- Added `base_currency` (String 3, default 'GBP', server_default 'GBP') to `User` model in `database.py`; plaintext column (not sensitive)
- Added `currency` (String 3, default 'GBP', server_default 'GBP') to `MonthlyExpense` model in `database.py`; tracks the ISO 4217 currency of each expense
- Added `ExchangeRate` ORM model to `database.py`: `id`, `base` (String 3, indexed), `target` (String 3, indexed), `rate` (Float), `date` (Date, indexed), `created_at`; unique constraint on (base, target, date); stores EUR-based rates from Frankfurter API to allow cross-currency math
- Added `Date`, `UniqueConstraint` to `database.py` SQLAlchemy imports
- Created Alembic migration `h8i9j0k1l2m3_add_multi_currency.py`: adds `users.base_currency`, `monthly_expenses.currency`, creates `exchange_rates` table with 4 indexes; `down_revision` = `g7h8i9j0k1l2`
- Created `routers/currency.py`: 25-currency SUPPORTED_CURRENCIES list (ISO 4217 with symbol); `GET /currency/list` returns static currency list; `GET /currency/rates?base=GBP` returns latest rates from DB (cold-start inline fetch if none), computed via EUR cross-rates; `sync_exchange_rates(db_factory)` background function fetching from Frankfurter API (`https://api.frankfurter.app/latest?from=EUR`) with open.er-api.com fallback; `_fetch_and_store_rates(db)` upserts rate rows by (base, target, date)
- Updated `routers/users.py`: added `base_currency: str` to `UserProfileResponse`; added `base_currency: str | None = None` to `UpdateProfileRequest`; `update_profile` validates and updates `base_currency` (rejects unknown ISO codes); `get_profile` returns `base_currency`
- Updated `models.py`: added `currency: Optional[str] = None` to `ExpenseUpdateRequest`; added `currency: str = "GBP"` to `ExpenseResponse`
- Updated `main.py`: imported `ExchangeRate`, `currency_router`; added `currency: Optional[str] = None` to `ActualExpenseItem` schema; updated `save_actuals` to persist `currency` per item (defaults to user's base_currency); updated `get_monthly_tracker` to include `currency` per expense dict and `base_currency` in response; updated `update_expense` to validate and update `currency` field; updated `_expense_snapshot` to include `currency`; updated `calculate_budget` to set `currency` on new expenses; registered `currency_router`; added `sync_exchange_rates` APScheduler job at 00:15 UTC
- Created `web/src/api/currency.js`: `getCurrencies()`, `getExchangeRates(base)` Axios wrappers
- Created `web/src/hooks/useCurrency.js`: `useCurrencies()` (staleTime Infinity), `useExchangeRates(base)` (1h staleTime), `convertCurrency(amount, from, to, rates)` helper, `currencySymbol(code)` lookup map
- Updated `web/src/components/AccountSettings.jsx`: imported `useCurrencies`; added `baseCurrencyInput` state synced from profile; added currency selector (select dropdown with all 25 currencies); saves `base_currency` alongside username on profile save
- Updated `web/src/components/MonthlyTracker.jsx`: imported `useProfile`, `currencySymbol`, `useCurrencies`; `baseCurrency` derived from profile (fallback 'GBP'); `sym` = dynamic currency symbol; `buildRowsFromExpenses` now includes `currency` per row; `addRow` uses `baseCurrency` as default; `startEdit`/`saveEdit` include currency in draft/payload; mobile card form shows per-row currency selector (visible in edit/new mode, badge for foreign currencies); desktop table has Currency column with selector (edit/new) or code badge (readonly); all `£` symbols replaced with dynamic `sym` or `currencySymbol(row.currency)`; Pie chart tooltip and summary section use `sym`; AuditDrawer uses `currencySymbol(snapshot.currency)` for amounts
- Updated `web/src/hooks/useExpenses.js`: `useSaveMonthlyTracker` passes `currency` in actualExpenses items when present
- Test result: 94 backend tests pass unchanged; frontend build clean (244.31 kB gzip, +889 B, 0 errors)
- Files changed: database.py, alembic/versions/h8i9j0k1l2m3_add_multi_currency.py (new), routers/currency.py (new), routers/users.py, models.py, main.py, web/src/api/currency.js (new), web/src/hooks/useCurrency.js (new), web/src/components/AccountSettings.jsx, web/src/components/MonthlyTracker.jsx, web/src/hooks/useExpenses.js

## Phase 5.1 — Docker and Docker Compose [DONE] 2026-04-06
- Created multi-stage `Dockerfile` for the backend: stage 1 (`builder`) installs gcc/libpq-dev and pip-installs all requirements into `/install`; stage 2 (`runtime`) uses `python:3.11-slim`, copies the prefix, creates a non-root `appuser:appgroup` (UID/GID 1001), copies application source with correct ownership, and runs `alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000`
- Created `web/nginx.conf`: serves static assets with `Cache-Control: public, immutable` (1-year expiry), falls back all routes to `index.html` for client-side hash routing, adds `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` headers, disables `server_tokens`
- Created multi-stage `web/Dockerfile`: stage 1 (`builder`) runs `npm ci --legacy-peer-deps` and `npm run build` (accepting `REACT_APP_API_URL` build arg); stage 2 (`runtime`) serves the built assets via `nginx:1.25-alpine` with the custom config
- Created `web/Dockerfile.dev`: single-stage Node 20 Alpine image that runs `npm start` for hot-reload
- Created `docker-compose.yml` with four services: `db` (postgres:16-alpine, health-checked with `pg_isready`), `redis` (redis:7-alpine, health-checked with `redis-cli ping`), `backend` (health-checked via `GET /health`, depends on db/redis healthy), `frontend` (nginx, depends on backend healthy); secrets via env vars; named volumes `postgres_data` and `redis_data`
- Created `docker-compose.override.yml` (auto-applied in dev): overrides backend `command` to add `--reload` flag and sets `LOG_LEVEL=DEBUG`; overrides frontend service to use `Dockerfile.dev` on port 3000 with source-code volume mounts and polling-based file watching
- Created `.dockerignore` (backend) and `web/.dockerignore` (frontend) to exclude venvs, test artifacts, `.env` files, `node_modules`, and `build/` directories
- Added `GET /health` endpoint to `main.py`: performs `SELECT 1` via SQLAlchemy `text()` to verify DB connectivity; returns `{"status": "ok", "version": "1.0.0"}`; used by Docker healthcheck and pre-wired for Railway health checks (Phase 5.3)
- Tests: 94 backend tests pass (unchanged), 7 deprecation warnings (pre-existing)

## Phase 5.2 — CI/CD Pipeline (GitHub Actions) [DONE] 2026-04-06
- Created `.github/workflows/ci.yml`: 4-job pipeline triggered on every PR and push to main — `backend-test` runs `python -m pytest --cov=. --cov-fail-under=65 --cov-report=xml` with artifact upload; `frontend-test` runs `CI=true npm test -- --watchAll=false --forceExit`; `lint` runs `ruff check . --exclude alembic/` and `npx eslint src/ --ext .js,.jsx --max-warnings 50`; `security` runs `bandit -r . --exclude ./tests,./alembic -ll -q` and `npm audit --audit-level=high`
- Created `.github/workflows/deploy.yml`: triggered by `workflow_run` on CI completion for main branch, requires `conclusion == 'success'`; installs Railway CLI, runs `railway up --service backend --environment production --detach`, waits 30s, smoke-tests `GET /health`
- Created `ruff.toml`: ignores E402 (intentional env-var-before-import ordering), E701/E702 (inline style), E711/E712 (SQLAlchemy `== None`/`== True` filter patterns); test files excluded from F401/F811
- Fixed 12 unused imports across `main.py`, `routers/audit.py`, `routers/currency.py`, `routers/overview.py`, `routers/savings.py`, `routers/signup.py`, `routers/users.py` (ruff --fix auto-applied)
- Fixed `tests/test_expenses.py:171` — removed assignment to unused `month` variable (F841)
- Added `# nosec B104` to `main.py:703` uvicorn `host="0.0.0.0"` — intentional server binding
- Added `ruff>=0.4.0` and `bandit[toml]>=1.7.0` to `requirements.txt`
- Added `pythonpath = .` to `pytest.ini` so `python -m pytest` resolves project modules without PYTHONPATH env var
- Added `jest.transformIgnorePatterns` to `web/package.json` to transform `axios` (v1.x ships as ESM; CRA/Jest doesn't handle ESM node_modules by default) — fixes MonthlyTracker and AuthGuard test suites which were failing with "Cannot use import statement outside a module"
- Added ESLint `overrides` to `web/package.json` disabling `import/first`, `testing-library/no-wait-for-multiple-assertions`, `testing-library/no-node-access` for test files (test patterns don't match ESLint's strict rules)
- Fixed `web/src/tests/MonthlyTracker.test.jsx` and `web/src/tests/AuthGuard.test.jsx`: moved component import statements to the top of each file — Babel requires all `import` statements at module level; having an `import` after `let` declarations caused Babel to skip transpilation
- Test results: 94 backend tests, 67% coverage (≥65% threshold met); 25 frontend tests across 5 suites (all passing); ruff 0 errors; bandit 0 medium/high issues; eslint 0 errors
- Files changed: .github/workflows/ci.yml (new), .github/workflows/deploy.yml (new), ruff.toml (new), requirements.txt, pytest.ini, web/package.json, tests/test_expenses.py, web/src/tests/MonthlyTracker.test.jsx, web/src/tests/AuthGuard.test.jsx, main.py (nosec + unused import fixes), routers/audit.py, routers/currency.py, routers/overview.py, routers/savings.py, routers/signup.py, routers/users.py

## Phase 5.3 — Monitoring and Error Tracking [DONE] 2026-04-06
- Added `sentry-sdk[fastapi]>=1.40.0` and `structlog>=24.1.0` to `requirements.txt`
- Added `SENTRY_DSN: str = ""` to `core/config.py` — empty string disables Sentry in local dev; set to a real DSN in production
- Rewrote `core/logging_config.py` to use structlog: in production (`ENVIRONMENT=production` or `LOG_FORMAT=json`), emits newline-delimited JSON via `structlog.processors.JSONRenderer` (compatible with Railway log drain / Datadog); stdlib logging redirected through a `ProcessorFormatter` so uvicorn/SQLAlchemy records appear in the same stream; in development emits human-readable coloured output via `structlog.dev.ConsoleRenderer`
- Updated `main.py`: imported `sentry_sdk`, `FastApiIntegration`, `SqlalchemyIntegration`; after `setup_logging()`, conditionally calls `sentry_sdk.init(dsn=settings.SENTRY_DSN, integrations=[FastApiIntegration(), SqlalchemyIntegration()], environment=settings.ENVIRONMENT, release="son-of-mervan@1.0.0", traces_sample_rate=1.0 in prod else 0.0)` — no-op when DSN is unset; logs a startup message confirming Sentry is active
- Added `"@sentry/react": "^8.0.0"` to `web/package.json` dependencies
- Updated `web/src/index.js`: imports `* as Sentry from '@sentry/react'`; conditionally calls `Sentry.init(...)` with `REACT_APP_SENTRY_DSN`, `REACT_APP_ENVIRONMENT`, `REACT_APP_VERSION` build-time env vars and `browserTracingIntegration()`; no-op when DSN env var is absent
- Test results: 94 backend tests pass unchanged; frontend build clean (281.76 kB gzip, +37 kB from Sentry bundle)
- Files changed: requirements.txt, core/config.py, core/logging_config.py, main.py, web/package.json, web/src/index.js

## Phase 5.4 — Database Backups [DONE] 2026-04-06
- Created `scripts/backup.py`: runs `pg_dump` against `DATABASE_URL`, gzip-compresses the output, uploads to Cloudflare R2 (S3-compatible via boto3); writes a daily key `daily/son-of-mervan-YYYY-MM-DD.sql.gz` on every run, and an additional monthly key `monthly/son-of-mervan-YYYY-MM.sql.gz` on the 1st of each month; applies retention (30 daily, 12 monthly) by listing objects under each prefix, sorting by LastModified, and deleting extras; sends a failure alert email via SendGrid to `BACKUP_ALERT_EMAIL` if any step raises (console log fallback when `SENDGRID_API_KEY` is absent)
- Created `scripts/restore.py`: two subcommands — `list` (prints all backups in R2 grouped by daily/monthly, showing size and timestamp) and `restore <key>` (downloads the object, decompresses, pipes SQL to `psql --single-transaction`); `--dry-run` flag logs what would happen without touching the DB; `--yes` flag skips the interactive "type 'yes'" safety prompt
- Added `boto3>=1.34.0` to `requirements.txt`
- Added R2 and backup-alert env vars to `.env.example`: `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME` (default: `son-of-mervan-backups`), `BACKUP_ALERT_EMAIL`
- Deployment: schedule in Railway with cron expression `0 3 * * *` pointing to `python scripts/backup.py`; both scripts require `pg_dump`/`psql` binaries on PATH (available in the backend Docker image)
- Test result: 94 backend tests pass unchanged
- Files changed: scripts/backup.py (new), scripts/restore.py (new), requirements.txt, .env.example

## Phase 5.5 — Performance Optimisation [DONE] 2026-04-06
- Created `core/cache.py`: Redis-backed cache helpers (`cache_get`, `cache_set`, `cache_delete`, `invalidate_annual_cache`) with lazy client initialisation and graceful degradation — all operations silently no-op when `REDIS_URL` is unset or Redis is unreachable; connection errors logged as warnings, not raised
- Added `REDIS_URL: str = ""` to `core/config.py`; added `REDIS_URL=redis://localhost:6379/0` to `.env.example` with explanatory comment
- Added `redis>=5.0.0` to `requirements.txt`
- Updated `database.py`: PostgreSQL engine now sets `pool_size=10, max_overflow=20` (previously used SQLAlchemy defaults); added `Index("ix_monthly_data_user_id", "user_id")` to `MonthlyData.__table_args__` for fast user-scoped month queries; added `Index` to SQLAlchemy imports
- Created `alembic/versions/i9j0k1l2m3n4_add_performance_indexes.py`: `upgrade` creates `ix_monthly_data_user_id` on `monthly_data.user_id`; `downgrade` drops it. (Composite index on `(monthly_expenses.monthly_data_id, deleted_at)` was already added in e5f6a7b8c9d0.)
- Updated `routers/overview.py`: `GET /overview/annual` checks Redis cache first (key `annual:{user_id}:{year}`, TTL 3600s); serves cached result on hit; stores result after computation on miss
- Updated `main.py`: imported `invalidate_annual_cache`; added `_invalidate_expense_year_cache(db, user_id, monthly_data_id)` helper (looks up `MonthlyData` to extract year); added cache invalidation calls after commits in `POST /calculate-budget` (commit=True path), `POST /monthly-tracker/{month}`, `PUT /expenses/{id}`, and `DELETE /expenses/{id}`
- Updated `routers/insights.py`: fixed N+1 query pattern in `GET /insights/trends` — replaced per-month `db.query(MonthlyExpense)` loop with a single `selectinload(MonthlyData.expenses)` on the initial `MonthlyData` query; expenses now accessed as `row.expenses` filtered by `deleted_at is None` (eliminates up to 24 extra DB queries for the max lookback window)
- React Query `staleTime` was already set correctly in prior phases: `useAnnualSummary` = 5 min, `useMonthlyTracker` = 30s — no frontend changes needed
- Test result: 94 backend tests pass, 7 pre-existing deprecation warnings; ruff 0 errors
- Files changed: core/cache.py (new), core/config.py, .env.example, requirements.txt, database.py, alembic/versions/i9j0k1l2m3n4_add_performance_indexes.py (new), routers/overview.py, routers/insights.py, main.py

## Phase 6.2 — Raise CI Coverage Threshold to 80% [DONE] 2026-04-07
- Added 5 new test files covering the previously untested router modules (alerts, export, insights, recurring, savings):
  - `tests/test_alerts.py`: 27 tests covering budget alert CRUD, ownership isolation, notification list/mark-read/mark-all-read/delete
  - `tests/test_export.py`: 9 tests covering CSV export (with and without data, wrong-user isolation, invalid month format) and PDF export (200/503 depending on fpdf2 availability, 404 for missing month, invalid format)
  - `tests/test_insights.py`: 15 tests covering monthly-summary, trends (with custom `months` param and validation), and heatmap endpoints
  - `tests/test_recurring.py`: 21 tests covering recurring expense CRUD, frequency validation, ownership (403 for wrong user), and the `/generate` endpoint
  - `tests/test_savings.py`: 32 tests covering savings goal CRUD, contributions (add/delete, current_amount aggregation), and ownership isolation (404 for wrong user)
- Updated `.github/workflows/ci.yml`: `--cov-fail-under` raised from 65 to 80
- Test result: 198 tests pass (was 94), 85.84% total line coverage (was 67%), all 7 pre-existing deprecation warnings unchanged
- Files changed: tests/test_alerts.py (new), tests/test_export.py (new), tests/test_insights.py (new), tests/test_recurring.py (new), tests/test_savings.py (new), .github/workflows/ci.yml

## Phase 6.3 — Documentation Refresh [DONE] 2026-04-07
- Rewrote root `CLAUDE.md`: updated tech stack (15-min access tokens + 30-day refresh, React Query, Zustand, React Router v6); added full project layout including `core/`, `scripts/`, `alembic/`, and `web/src/{api,hooks,store,context,styles}`; updated all 25+ key API endpoints table; expanded database models to include all 12 models (RefreshToken, PasswordResetToken, RecurringExpense, SavingsGoal/Contribution, BudgetAlert, Notification, AuditLog, ExchangeRate); rewrote auth flow to reflect 401 interceptor + in-memory token; added APScheduler jobs table; updated env vars (JWT_SECRET now required/no fallback, added REDIS_URL/SENTRY_DSN/R2 vars); added Docker and CI/CD sections; replaced stale known-issues with accurate current tech debt
- Rewrote `routers/CLAUDE.md`: added all 8 new routers (users, recurring, savings, alerts, insights, export, audit, currency) with full endpoint tables and implementation notes; kept tracker.py with accurate "legacy/candidate for removal" status
- Rewrote `web/CLAUDE.md`: replaced entire stale file — updated stack description (React Router v6, React Query, Zustand); complete source layout tree including all api/, hooks/, store/, context/, styles/, and tests/ files with one-line descriptions; updated routing section with all 9 protected routes; rewrote auth state section (in-memory token, Zustand, refresh interceptor); replaced "known issue" API_BASE_URL section with correct env var configuration; replaced "plain hooks" state management section with React Query + Zustand description; added dark mode section; updated key data flows with edit/delete/audit/export additions
- Files changed: CLAUDE.md, routers/CLAUDE.md, web/CLAUDE.md, PRD.md

## Phase 7.4 — Weekly Spending Pace Indicator [DONE] 2026-04-07
- Added `GET /insights/pace?month=YYYY-MM` endpoint in `routers/insights.py`:
  - Computes linear projection: `(actual_so_far / days_elapsed) × days_in_month` per category and overall
  - Handles edge cases: future month (0 days elapsed, empty warnings), past month (days_elapsed = full month), current month uses `datetime.utcnow().day`
  - Returns `{ month, days_elapsed, days_in_month, categories:{…}, overall:{…}, warnings:[…] }`
  - A warning is raised for any category where `projected > planned × 1.10` (>10% over budget)
  - Warning message format: "At this pace you'll overspend {Category} by £{amount:.2f} by month end."
- Added `getSpendingPace(month)` Axios wrapper in `web/src/api/insights.js`
- Added `useSpendingPace(month)` React Query hook in `web/src/hooks/useInsights.js` (staleTime 2 min)
- Added amber warning banner in `web/src/components/MonthlyTracker.jsx`:
  - Positioned between the category filter bar and the salary input
  - Renders only when `paceData.warnings.length > 0`
  - Lists each category warning message; accessible (role=alert, aria-live=polite)
  - Dark-mode compatible amber styling
- Added 8 new tests in `tests/test_insights.py` (class `TestSpendingPace`):
  - Missing/invalid month → 422; unauthenticated → 401/403
  - Empty month, future month, over-budget data, within-budget data, cross-user isolation
- Test result: 206 tests pass (was 198), 86.16% coverage (was 85.84%)
- Files changed: routers/insights.py, web/src/api/insights.js, web/src/hooks/useInsights.js, web/src/components/MonthlyTracker.jsx, tests/test_insights.py, PRD.md, progress.txt

## Phase 7.6 — Smart Categorisation [DONE] 2026-04-07
- Added `GET /insights/suggest-category?name=<text>` endpoint in `routers/insights.py`:
  - `name` query param required, min 2 chars (FastAPI validates; returns 422 if < 2 chars)
  - Fetches all non-deleted MonthlyExpense records for the authenticated user
  - Case-insensitive substring match: "tesco" matches "Tesco", "Tesco Express", "Tesco Metro"
  - Counts category frequency across all matches; returns the most common
  - Returns `{ suggestion, count, total_matches }` — `suggestion: null` when no history matches
  - No cross-user data: only queries the authenticated user's own MonthlyData IDs
- Added `suggestCategory(name)` Axios wrapper in `web/src/api/insights.js`
- Added `useCategorySuggestion(name)` React Query hook in `web/src/hooks/useInsights.js`:
  - `enabled: trimmed.length >= 2`; `staleTime: 60s`; `refetchOnWindowFocus: false`
- Added `CategorySuggestionChip` component in `web/src/components/MonthlyTracker.jsx`:
  - Self-contained: debounces the name prop 300ms, calls `useCategorySuggestion` internally
  - Renders a small pill button "Suggested: {category}" when suggestion exists; null otherwise
  - `aria-label` and `title` (shows past occurrence count) for accessibility
  - Wired into both mobile card layout and desktop table layout, for new rows (id==null) and
    edit mode (isEditing), clicking the chip updates the category field
- Added inline suggestion chip in `RecurringExpensesPage.jsx` FormRow:
  - 300ms debounce via `useState`/`useEffect` on `value.name`
  - Chip renders below the category `<select>` when `categorySuggestion.suggestion` is set
- Added 8 new tests in `tests/test_insights.py` (class `TestSuggestCategory`):
  - Missing name → 422; name too short (1 char) → 422; unauthenticated → 401/403
  - No history → null suggestion; most-common category chosen correctly
  - Case-insensitive matching; cross-user isolation; deleted expenses excluded
- Test result: 214 tests pass (was 206), 86.41% coverage (was 86.16%)
- Files changed: routers/insights.py, web/src/api/insights.js, web/src/hooks/useInsights.js, web/src/components/MonthlyTracker.jsx, web/src/components/RecurringExpensesPage.jsx, tests/test_insights.py, PRD.md, progress.txt

## Phase 7.5 — Financial Health Score [DONE] 2026-04-07
- Added `GET /insights/health-score?month=YYYY-MM` endpoint in `routers/insights.py`
- Score is 0–100, composed of three weighted components:
  - Savings rate (40%): `(salary_actual - total_actual) / salary_actual × 100`; 20%+ = 100 pts
  - Budget adherence (30%): % of categories with planned budget that stayed within it; 0 when no data
  - Emergency fund (30%): months of expenses covered by savings goal contributions; 3 months = 100 pts
- Score bands: red 0–39, amber 40–69, green 70–100
- Returns `{ month, score, band, components: {savings_rate, budget_adherence, emergency_fund}, explanations }`
- Score=0 with no data confirmed (all components 0; budget adherence neutral value changed to 0 not 50)
- Imported `SavingsGoal`, `SavingsContribution` from `database` into insights router
- Added `getHealthScore(month)` Axios wrapper in `web/src/api/insights.js`
- Added `useHealthScore(month)` React Query hook in `web/src/hooks/useInsights.js` (staleTime 5 min)
- Added `HealthScoreCard` component in `web/src/components/InsightsPage.jsx`:
  - Rendered at the top of the page, tied to the existing `selectedMonth` state
  - Score dial (large number + filled progress bar) with colour-coded band label
  - Three `ComponentRow` entries with per-component scores, weights, progress bars, and detail text
  - Plain-English explanations list at the bottom
  - Skeleton loader while data fetches
- Added 11 new tests in `tests/test_insights.py` (class `TestHealthScore`):
  - No data → score=0, band=red; missing/invalid month → 422; unauthenticated → 401/403
  - High savings rate gives savings_component=100; all-within-budget gives adherence=100
  - Overspending reduces adherence to 50%; savings contributions give emergency_fund=100
  - Full response structure validated; cross-user data isolation confirmed
- Test result: 225 tests pass (was 214), 87.04% coverage (was 86.41%)
- Files changed: routers/insights.py, tests/test_insights.py, web/src/api/insights.js, web/src/hooks/useInsights.js, web/src/components/InsightsPage.jsx, PRD.md, progress.txt

**Phase 9.1 — Monthly Budget Email Digest [DONE] 2026-04-08**
- Added `digest_enabled` (bool, default True) column to `User` model; Alembic migration `j0k1l2m3n4o5`
- `send_monthly_digest_email()` added to `email_utils.py` — plain-text email with: month label, income, total spent, savings rate, top 3 categories, over-budget categories; graceful no-op when `SENDGRID_API_KEY` not set
- `send_monthly_digests(session_factory)` function in `main.py` — iterates all opted-in, non-deleted users; skips users with no data for previous month; aggregates category totals and over-budget flags from actual vs planned amounts
- APScheduler job registered on day=1, hour=8 UTC; logged in startup banner
- `GET /users/me` and `PUT /users/me` extended with `digest_enabled` field; `UpdateProfileRequest.digest_enabled` is optional so existing callers are unaffected
- `AccountSettings.jsx` gains "Email Notifications" section with accessible toggle switch (role=switch, aria-checked); auto-saves on click via `updateProfileMutation`
- 12 new tests in `TestDigestEnabledField`, `TestSendMonthlyDigests`, `TestSendMonthlyDigestEmailDevMode` covering: default true, disable/re-enable persistence, skip no-data, skip opted-out, skip deleted, over-budget detection, multi-user send, dev-mode logging
- 244 backend tests pass, 86.68% coverage; ruff clean on all changed files
- Files: `database.py`, `alembic/versions/j0k1l2m3n4o5_*.py`, `email_utils.py`, `main.py`, `routers/users.py`, `web/src/components/AccountSettings.jsx`, `tests/test_digest.py`, `PRD.md`, `progress.txt`

## Phase 9.2 — Onboarding Wizard [DONE] 2026-04-08
- Added `has_completed_onboarding` (bool, default False) to `User` model in `database.py`
- Alembic migration `l2m3n4o5p6q7_add_onboarding_flag.py`:
  - Adds column with `server_default='0'` for new users
  - Data migration sets all *existing* users to `1` so they are not redirected to the wizard after deploy
- `UserProfileResponse` extended with `has_completed_onboarding: bool`
- `UpdateProfileRequest` extended with `has_completed_onboarding: bool | None = None`
- `GET /users/me` and `PUT /users/me` both handle the new field; `bool()` cast for SQLite compatibility
- `useProfile(options = {})` in `hooks/useProfile.js` now accepts React Query option overrides (used by AuthGuard to set `enabled`)
- `OnboardingWizard.jsx` — 3-step wizard at `/onboarding`:
  - Step 1: Welcome screen with 4 app feature highlights and "Get started" CTA + "Skip" link
  - Step 2: Currency selector (from `/currency/list`), monthly income input (validated ≥ 0), category tile checkboxes (8 presets: Housing, Transportation, Food, Utilities, Insurance, Healthcare, Entertainment, Other)
  - Step 3: Confirmation summary (currency, income, selected categories) + "Start Budgeting" button
  - On complete: `POST /calculate-budget?commit=true` (current month, all categories at £0) then `PUT /users/me { base_currency, has_completed_onboarding: true }`; React Query profile cache updated via `setQueryData`; navigate to `/budget` with success toast
  - Auth guard built-in: redirects to `/login` if unauthenticated, to `/budget` if already completed
- `router.jsx` — `/onboarding` added as a top-level route (outside AuthGuard, has its own auth/state logic)
- `AuthGuard.jsx` — imports `useProfile`; fetches profile after session restore with `enabled: isAuthenticated && !loading`; redirects to `/onboarding` when `has_completed_onboarding === false`; shows spinner while profile loads for first time (cached for 5 min after)
- 9 new tests in `tests/test_onboarding.py` (class `TestOnboardingFlag`):
  - Response includes field; default false for new users; PUT persists true; GET reflects persisted value; can reset to false; preserves other fields; omitting field leaves it unchanged; unauthenticated GET/PUT → 401/403
- Test result: 274 tests pass (was 244), 87.09% coverage (was 86.68%)
- Files changed: database.py, alembic/versions/l2m3n4o5p6q7_add_onboarding_flag.py, routers/users.py, web/src/hooks/useProfile.js, web/src/components/OnboardingWizard.jsx, web/src/router.jsx, web/src/components/AuthGuard.jsx, tests/test_onboarding.py, PRD.md, progress.txt

## Phase 9.4 — Budget Templates [DONE] 2026-04-08
- Added `BUDGET_TEMPLATES` constant to `web/src/components/SonOfMervan.jsx` with 4 templates:
  - **50/30/20 Rule**: Housing 25%, Groceries 10%, Transport 8%, Utilities 4%, Insurance 3%, Entertainment 15%, Healthcare 5%, Other 10% → 20% savings
  - **Zero-Based**: Housing 30%, Groceries 12%, Transport 10%, Utilities 6%, Insurance 5%, Healthcare 7%, Entertainment 10%, Savings 15%, Other 5% → 0% unallocated
  - **Minimalist**: Housing 35%, Groceries 15%, Transport 10%, Utilities 5%, Other 5% → 30% savings
  - **Student Budget**: Rent 40%, Groceries 20%, Transport 10%, Entertainment 8%, Healthcare 5%, Other 7% → 10% savings
- "Use Template" button (purple, LayoutTemplate icon) added next to "Add row" button in the expense section header
- Clicking opens a modal (`role="dialog"`) with template cards: name, description, savings-% badge, category pill tags with percentages, and a "Use this template" CTA
- `applyTemplate(template)` replaces expense rows with template rows; if salary is already entered, amounts are pre-calculated as `Math.round(salary × pct / 100)`; otherwise amounts are left blank for the user to fill
- Modal dismisses on template selection, close button click, or backdrop click
- 7 new frontend tests in `web/src/tests/SonOfMervan.test.jsx` (13 total, all passing):
  - Button visible; modal opens on click; all four templates shown; close button works; 50/30/20 populates rows; salary-based amounts calculated correctly; no salary → blank amounts
- No backend changes required (templates are hardcoded frontend constants)
- Files changed: web/src/components/SonOfMervan.jsx, web/src/tests/SonOfMervan.test.jsx, PRD.md, progress.txt

---

## Phase 8.2: Multi-User Household Accounts [2026-04-08]

**Goal:** Allow couples and housemates to share a budget view, with an owner/member role model and invite-based membership.

**Backend:**
- `Household`, `HouseholdMembership`, `HouseholdInvite` models added to `database.py`
  - `Household`: id, owner_id (FK → users), name, created_at, deleted_at
  - `HouseholdMembership`: id, household_id, user_id, role (owner|member), joined_at; UniqueConstraint(household_id, user_id)
  - `HouseholdInvite`: id, household_id, invitee_email, token_hash (SHA-256), expires_at, accepted_at
- Alembic migration `137017825f82_add_household_tables.py` — creates all three tables with appropriate indexes
- `routers/household.py` — 7 endpoints under `/households` prefix:
  - `POST /households` — create household; caller becomes owner; one household per user
  - `GET /households/me` — return household with members + pending invites
  - `POST /households/invite` — owner sends 7-day invite by email (SendGrid in prod, log in dev); old pending invites for same email are expired
  - `POST /households/join` — accept invite via raw token; validates email match, expiry, not already a member
  - `DELETE /households/members/{user_id}` — owner removes a member (cannot remove self)
  - `DELETE /households` — owner soft-deletes household (all members lose access)
  - `GET /households/budget?month=YYYY-MM` — combined budget view: per-member MonthlyData aggregated into combined totals
- Pydantic schemas added to `models.py`: `HouseholdCreate`, `HouseholdInviteRequest`, `HouseholdJoinRequest`, `MemberResponse`, `HouseholdResponse`, `HouseholdBudgetMemberSummary`, `HouseholdBudgetResponse`
- `_send_household_invite_email()` in router (same SendGrid pattern as other email utils)
- Security model: owner-only actions enforced server-side; invite token is opaque (only hash stored); email-address binding on join

**Frontend:**
- `web/src/api/household.js` — 7 API functions (getMyHousehold, createHousehold, inviteMember, joinHousehold, removeMember, dissolveHousehold, getHouseholdBudget)
- `web/src/hooks/useHousehold.js` — React Query hooks for all operations; 404 on `useMyHousehold` does not retry (expected when no household)
- `web/src/components/HouseholdPage.jsx`:
  - Empty state: create-household form (name input + submit)
  - Member list with role badges (Owner=indigo, Member=gray), avatar initials, remove button for non-owner members (owner only)
  - Invite panel (owner only): email input, send invite, pending invites list with amber dot
  - Collapsible combined budget view: month picker, combined KPI cards (income/expenses/remaining actual vs planned), per-member breakdown cards
  - Danger zone (owner only): dissolve household with ConfirmModal confirmation
  - Remove-member flow also uses ConfirmModal
- Route `/household` added to `router.jsx`
- "Household" nav tab (Users icon) added to desktop sidebar and mobile bottom bar in `AuthGuard.jsx`

**Tests:**
- `tests/test_household.py` — 19 new tests:
  - Create household, empty name → 422, duplicate → 409
  - Get household not member → 404, owner → 200
  - Invite member, invite self → 422, non-owner invite → 403
  - Join: success, expired → 410, wrong email → 403, already member → 409
  - Remove member, remove self → 422, member cannot remove → 403
  - Dissolve: owner success, member cannot → 403
  - Budget view: combined totals aggregated correctly, missing month → zeros
- All 303 tests pass, 86.66% coverage (exceeds 80% target)
- Build: 305.12 kB gzip (clean, no warnings)
- Files changed: database.py, models.py, main.py, routers/household.py (new), alembic/versions/137017825f82_add_household_tables.py (new), tests/test_household.py (new), web/src/api/household.js (new), web/src/hooks/useHousehold.js (new), web/src/components/HouseholdPage.jsx (new), web/src/router.jsx, web/src/components/AuthGuard.jsx, PRD.md, progress.txt

## Phase 11.4 — Debt Payoff Calculator [DONE] 2026-04-08
- `database.py`: Added `Debt` model — `id`, `user_id` (FK), `interest_rate` (plaintext float, annual APR as decimal), `minimum_payment` (plaintext float), `created_at`, `deleted_at`; encrypted fields: `_name_encrypted` (`name` hybrid property), `_balance_encrypted` (`balance` hybrid property returning float)
- `models.py`: Added `DebtCreate`, `DebtUpdate`, `DebtResponse`, `PayoffDebtMonth`, `PayoffMonth`, `PayoffPlanResponse` Pydantic schemas
- `routers/debts.py` (new): Full CRUD `GET/POST/PUT/DELETE /debts` + `GET /debts/payoff-plan?strategy=snowball|avalanche`. Payoff plan simulation: applies monthly interest to all live debts, pays minimum on each, rolls freed minimums to top-priority debt (sorted by ascending balance for snowball, descending rate for avalanche). Capped at 360 months. Returns `{ strategy, months: [{month, debts: [{name, remaining_balance}]}], total_interest_paid, payoff_months }`.
- `main.py`: Imported and registered `debts_router`
- `alembic/versions/n4o5p6q7r8s9_add_debts_table.py`: Migration creating `debts` table with indexes on `id` and `user_id`
- `web/src/api/debts.js`: `listDebts`, `createDebt`, `updateDebt`, `deleteDebt`, `getPayoffPlan` Axios wrappers
- `web/src/hooks/useDebts.js`: `useDebts`, `usePayoffPlan`, `useCreateDebt`, `useUpdateDebt`, `useDeleteDebt` React Query hooks
- `web/src/components/DebtPayoffPage.jsx`: Debt list table with edit/delete per row; add-debt modal (name, balance, APR%, minimum payment); 4 KPI cards (total owed, monthly minimums, months to freedom, total interest); payoff timeline Recharts LineChart with one line per debt; avalanche/snowball strategy toggle; empty state; ConfirmModal for delete
- `web/src/router.jsx`: Added `/debts` route → `DebtPayoffPage`
- `web/src/components/AuthGuard.jsx`: Added `TrendingDown` lucide icon; added `["/debts", "Debt Payoff", TrendingDown]` to `NAV_TABS`
- `tests/test_debts.py`: 17 tests — list empty, list own debts, excludes deleted, excludes other users, create 201, negative balance 422, invalid rate 422, zero minimum 422, update balance, update wrong user 404, delete 204, delete wrong user 404, empty payoff plan, invalid strategy 422, single-debt snowball pays off, avalanche less interest than snowball, snowball orders lowest balance first
- All 403 backend tests pass (17 new); 88.50% coverage; ruff clean (excluding alembic/); build 317 kB gzip

## Phase 11.5 — Spending Streak & Habit Tracker [DONE] 2026-04-08
- `routers/insights.py`: Added `GET /insights/streaks` endpoint. Fetches all `MonthlyData` for user, filters to months with `total_actual > 0` (tracked months), sorts chronologically, computes `current_streak` (consecutive under-budget months at end of history) and `longest_streak` (max run ever). Returns `{ current_streak, longest_streak, total_tracked, months_under }`. Months with no actual data are skipped entirely (don't break streak).
- `web/src/api/insights.js`: Added `getStreaks()` — `GET /insights/streaks` Axios wrapper
- `web/src/hooks/useInsights.js`: Added `useStreaks()` React Query hook (staleTime 5 min)
- `web/src/components/SonOfMervan.jsx`: Added `useStreaks()` hook; flame badge displayed above Inputs card when `current_streak > 0` (shows streak count, month label, best-ever badge if longest > current); `useEffect` + `useRef` fires `toast.success()` milestone toasts at 3, 6, 12 months (fires once per mount per milestone level reached)
- `tests/test_insights.py`: 7 new tests — unauthenticated 401/403, no data zero streaks, single under-budget month streak=1, three consecutive months streak=3, over-budget month resets current but preserves longest, months with no actuals skipped (streak maintained across gap), data isolation from other users
- 410 backend tests pass (7 new); 88.63% coverage; ruff clean

## Phase 12.1 — Budget Copy Forward [DONE] 2026-04-08
- `web/src/components/SonOfMervan.jsx`: Added `loadingPrevMonth` state, `prevMonthLabel` computed value (YYYY-MM string + display string for current prev month, handles year boundary), `loadPrevMonth` async function (calls `getMonthlyTracker(prevMonth, { page_size: 100 })`, sets salary + expense rows from response, shows success/info/error toasts, disables button while loading). Added "Load [prev month]" button before "Use Template" button in expense row controls toolbar. Added imports for `getMonthlyTracker` from `../api/expenses` and `toast` from `react-hot-toast`.
- `web/src/tests/SonOfMervan.test.jsx`: Added mocks for `../hooks/useCategories`, `../hooks/useInsights` (useStreaks), `../api/expenses` (getMonthlyTracker), and `react-hot-toast`. Updated `beforeEach` to set default `mockGetMonthlyTracker` response. Added `Budget Copy Forward` describe block with 3 tests: button renders, clicking pre-fills salary + expense rows from prev month data, no-data informational toast.
- `PRD.md`: Added Phase 12 (5 tasks: Budget Copy Forward, Net Worth Tracker, Accessibility, Full Data Export, Milestone Email Notifications); 12.1 marked DONE.
- 16 frontend SonOfMervan tests pass (3 new); 410 backend tests unchanged; 88.63% coverage

## Phase 12.2 — Net Worth Tracker [DONE] 2026-04-08
- `database.py`: Added `NetWorthSnapshot` ORM model (`net_worth_snapshots` table). Fields: `id`, `user_id` (FK), `snapshot_date` (Date, plaintext), `total_assets` / `total_liabilities` (Float, plaintext computed sums), `created_at`, `deleted_at`. Encrypted hybrid properties: `assets_json` / `liabilities_json` (Fernet-encrypted JSON arrays of `{name, value}` dicts, stored in `String(4096)` columns).
- `models.py`: Added `NetWorthItem`, `NetWorthSnapshotCreate`, `NetWorthSnapshotUpdate`, `NetWorthSnapshotResponse` Pydantic schemas. Added `date` to imports.
- `routers/net_worth.py`: New router (`/net-worth/snapshots` prefix). `GET` lists all non-deleted snapshots chronologically with computed `net_worth` field. `POST` validates at least one asset or liability, computes totals, encrypts JSON, returns 201. `PUT` supports partial updates of date/assets/liabilities with total recalculation. `DELETE` soft-deletes.
- `main.py`: Imported `net_worth as net_worth_router`; registered `net_worth_router.router` after debts router.
- `alembic/versions/o5p6q7r8s9t0_add_net_worth_snapshots.py`: Migration creating `net_worth_snapshots` table with indexes on `id` and `user_id`.
- `web/src/api/netWorth.js`: `listSnapshots`, `createSnapshot`, `updateSnapshot`, `deleteSnapshot` Axios wrappers.
- `web/src/hooks/useNetWorth.js`: `useNetWorthSnapshots`, `useCreateSnapshot`, `useUpdateSnapshot`, `useDeleteSnapshot` React Query hooks (staleTime 5 min).
- `web/src/components/NetWorthPage.jsx`: KPI cards (Net Worth, Total Assets, Total Liabilities) from latest snapshot; Recharts AreaChart showing net worth trend over time (only rendered when 2+ snapshots); collapsible snapshot history rows showing per-asset/liability breakdown; add-snapshot form with dynamic asset/liability item rows; soft-delete per snapshot. Theme-aware chart colours via `useTheme()`.
- `web/src/router.jsx`: Added `NetWorthPage` import; added `/net-worth` route inside AuthGuard children.
- `web/src/components/AuthGuard.jsx`: Added `BarChart3` lucide icon; added `["/net-worth", "Net Worth", BarChart3]` to `NAV_TABS`.
- `tests/test_net_worth.py`: 22 tests — list empty, list own snapshots, excludes deleted, excludes other users, net_worth field correct, chronological order, unauthenticated 401/403, create 201, response shape, assets-only, liabilities-only, empty body 422, missing date 422, update assets, update date, update not found 404, update other user 404, delete 204, soft-delete verified, delete not found 404, delete other user 404.
- 432 backend tests pass (22 new); 88.95% coverage; ruff clean on new files

## Phase 12.3 — Accessibility (WCAG 2.1 AA) [DONE] 2026-04-08
- `web/src/components/ConfirmModal.jsx`: Added `role="dialog"`, `aria-modal="true"`, `aria-labelledby` pointing to `id="confirm-modal-title"` on the h3. Added ESC key listener (closes modal) and focus trap (Tab/Shift+Tab cycle within modal). `confirmRef` auto-focuses the confirm button on open. Icon marked `aria-hidden="true"`. Both buttons get explicit `type="button"`.
- `web/src/components/AuthGuard.jsx`: NotificationPanel now uses `aria-modal="true"` and `aria-labelledby="notifications-panel-title"` (id added to h2). Close button gets `aria-label="Close notifications"`. Delete notification button gets `aria-label="Delete notification"`. Unread badge marked `aria-hidden="true"` since count is now in BellButton's dynamic `aria-label` (`"Notifications, N unread"`). Desktop nav buttons gain `aria-current="page"` to match mobile tab bar behaviour.
- `web/src/components/SonOfMervan.jsx`: Salary label associated with input via `htmlFor="budget-salary"` / `id="budget-salary"`. Currency symbol span marked `aria-hidden="true"`. Expense row name/amount/select inputs each get `aria-label="Expense N name/amount/category"` so they're distinguishable without visible column headers (which are hidden on mobile). Amount prefix spans marked `aria-hidden="true"`.
- `web/src/components/MonthlyTracker.jsx`: AuditDrawer dialog gets `aria-labelledby="audit-drawer-title"` (id added to Change History h2). Cross-month search input gets `aria-label="Search expense names"`. Search category select gets `aria-label="Filter search by category"`. Date range inputs get `aria-label="Search from month"` / `"Search to month"` (title attributes removed). Search icon marked `aria-hidden="true"`. Range separator span marked `aria-hidden="true"`. Filter-by-category label associated via `htmlFor="tracker-filter-category"`. Salary label associated via `htmlFor="tracker-salary"` / `id="tracker-salary"`.
- `web/src/api/netWorth.js`: Fixed pre-existing build-breaking bug — `import { client }` (named) corrected to `import client` (default export).
- `web/src/tests/MonthlyTracker.test.jsx`: Added `useExpenseSearch` mock (returns null data) so cross-month search feature doesn't crash in tests.
- `web/src/tests/AuthGuard.test.jsx`: Added `useProfile` and `useAlerts` mocks so AuthGuard renders nav (not spinner) in authenticated tests.
- 51 frontend tests pass (2 test file fixes, no new tests needed); build 320 kB gzip; 432 backend tests unchanged

## Phase 13.2 — TOTP Two-Factor Authentication (2FA) [DONE] 2026-04-08
- `requirements.txt`: Added `pyotp>=2.9.0` and `qrcode[pil]>=7.4.2`.
- `database.py`: Added `_totp_secret_encrypted` (Fernet-encrypted Column), `totp_enabled` (Boolean, default False) and `totp_secret` hybrid property (getter/setter/expression) to the `User` model.
- `alembic/versions/q7r8s9t0u1v2_add_totp_2fa.py`: Migration adds `totp_secret_encrypted` (String 512, nullable) and `totp_enabled` (Boolean, server_default "0") to `users`.
- `security.py`: Added `create_totp_challenge_token(user_id, email)` (5-min JWT, aud=totp-challenge, typ=totp_challenge) and `decode_totp_challenge_token(token)` with graceful error handling.
- `routers/totp.py` (new): 5 endpoints — `GET /auth/2fa/status`, `POST /auth/2fa/setup` (generates pyotp base32 secret, stores encrypted, returns `otpauth://` URI + base64 QR PNG via qrcode library), `POST /auth/2fa/confirm` (verifies first TOTP code, enables 2FA), `POST /auth/2fa/disable` (requires password + TOTP code, clears secret), `POST /auth/2fa/verify-login` (validates challenge JWT + TOTP code, issues full session with access token + 30-day httpOnly refresh cookie). All rate-limited via slowapi.
- `main.py`: Imported `create_totp_challenge_token` from security; modified `LoginResponse` to include optional `requires_2fa` and `totp_challenge_token`; login endpoint now short-circuits with `{requires_2fa: true, totp_challenge_token: <JWT>}` if `user.totp_enabled`; registered `totp_router`.
- `web/src/api/totp.js` (new): `getTOTPStatus`, `setupTOTP`, `confirmTOTP`, `disableTOTP`, `verifyTOTPLogin` Axios wrappers.
- `web/src/store/authStore.js`: Added `totpChallengeToken` state field, `setTOTPChallenge(token)` action; `handleLogin` and `clearAuth` clear `totpChallengeToken`.
- `web/src/components/TwoFactorPage.jsx` (new): Public route page shown after login when 2FA is required. Reads `totpChallengeToken` from store; submits code via `verifyTOTPLogin`; on success calls `handleLogin` and navigates to `/budget`; redirects to `/login` if no challenge token.
- `web/src/components/TwoFactorSetup.jsx` (new): Settings panel using React Query to display 2FA status and manage setup/disable flow. Setup: calls `setupTOTP`, shows QR image and manual URI, confirms with `confirmTOTP`. Disable: password + TOTP code form calling `disableTOTP`.
- `web/src/router.jsx`: Added lazy import for `TwoFactorPage`; registered public `/2fa` route.
- `web/src/components/LoginPage.jsx`: After login API call, checks `data.requires_2fa` — if truthy, calls `setTOTPChallenge(totp_challenge_token)` and navigates to `/2fa`.
- `web/src/components/AccountSettings.jsx`: Imported `TwoFactorSetup`; added "Two-Factor Authentication" `<Section>` between Security and Email Notifications sections.
- ruff: clean (1 spurious import caught and removed from totp.py); all backend file syntax OK

---
## Phase 13.3: Expense Notes & Tags [2026-04-08]

**Summary:** Added free-text `note` and `tags` (JSON array) to individual expenses for richer context and filtering.

**Backend changes:**
- `database.py`: Added `_note_encrypted` (String 1024) and `_tags_encrypted` (String 512) columns to `MonthlyExpense`. Added hybrid properties `note` and `tags` following the existing Fernet encrypt/decrypt pattern. Tags are stored as JSON-encoded lists.
- `alembic/versions/r8s9t0u1v2w3_add_notes_tags_to_expenses.py`: Migration adds `note_encrypted` and `tags_encrypted` columns to `monthly_expenses` table.
- `models.py`: Added `note: Optional[str]` (max 500 chars via Field) and `tags: Optional[List[str]]` to `ExpenseUpdateRequest`; added `note: Optional[str]` and `tags: List[str]` to `ExpenseResponse`.
- `main.py`: 
  - `PUT /expenses/{id}`: handles `note` and `tags` from request; tags silently capped at 5 items, each trimmed to 30 chars; both included in response.
  - `GET /monthly-tracker/{month}`: `note` and `tags` included in `expense_dicts`.
  - `GET /expenses/search`: new `tag` query param for exact-match tag filtering; `note` and `tags` included in search results.

**Frontend changes:**
- `web/src/components/MonthlyTracker.jsx`: 
  - `buildRowsFromExpenses` and the filtered-category path both now include `note` and `tags` when mapping API data to rows.
  - `startEdit` initialises `editDraft.note` and `editDraft.tags` from the row.
  - `saveEdit` includes `note` and `tags` in the `updateMutation` payload.
  - Mobile card view: note textarea and tags comma-input shown in edit mode; italic note text and blue tag chips shown in read mode.
  - Desktop table: new "Note / Tags" column with textarea+input in edit mode and chip display in read mode.

**Tests:**
- 6 new tests in `TestExpenseNotesTags` class in `tests/test_expenses.py`: note persist, tags persist, tags capped at 5, tracker response includes note/tags, search filters by tag, tag no-match returns empty.
- Total: 459 tests, 88.54% coverage (target: 80%).
- ruff: clean on all modified files.

---

## Phase 14.1 — Navigation Redesign (2026-04-08) ✅ DONE

**Problem solved:** 18 nav items crammed into a horizontal top bar made the app unusable on normal viewport widths and broken on mobile.

**What was built:**
- Replaced the overflowing top header nav with a collapsible left sidebar (desktop) and a 4-tab bottom bar with "More" sheet (mobile).
- `PRIMARY_NAV` (Budget, Tracker, Annual, Insights): always visible in sidebar primary section and mobile bottom tabs.
- `SECONDARY_NAV` (Recurring, Savings, Alerts, What-If, Portfolio, Calendar, Tax, Household, Categories, Import, Forecast, Debt Payoff, Net Worth): shown in a collapsible sidebar section (auto-expands when a secondary route is active) and in the mobile "More" bottom sheet.
- Settings, theme toggle, notification bell, and Logout moved to sidebar footer — no longer nav tabs.
- Mobile "More" sheet renders all secondary nav items in a 2-column grid plus Settings, theme toggle, and Logout at the bottom.
- Sidebar collapse state persisted to `localStorage` (`sidebarCollapsed` key).
- Notification slide-over panel retained; bell button in sidebar footer (desktop) and "More" badge indicator (mobile).
- Main content area offset via `sm:ml-60` / `sm:ml-16` matching sidebar width; mobile bottom padding clears the tab bar.
- Auto-expands secondary nav section when a secondary route is the current page.

**Files changed:**
- `web/src/components/AuthGuard.jsx`: complete rewrite — `Sidebar`, `SidebarNavItem`, `MobileTabBar`, `MobileMoreSheet` components; `NotificationPanel` and `BellButton` retained.

**Tests:**
- All 51 frontend tests pass (no new tests needed — existing AuthGuard suite covers auth, routing, logout, and aria-current).
- Commit: `70c15d9`

## Phase 14.2 — Budget Page Layout Overhaul [DONE] 2026-04-08

**Problem solved:** Single-column `max-w-3xl` form left vast empty space on wide screens; results only appeared after clicking Calculate.

**What was built:**
- `web/src/components/SonOfMervan.jsx`: Restructured to a `max-w-6xl` two-column layout (`lg:grid-cols-5` — form `col-span-3`, stats `col-span-2`). Added "Budget Planner / Plan your month" page header above the grid. Right column shows three live stat cards (Monthly Salary, Total Expenses, Monthly Savings / Budget Deficit) that compute instantly from form state as user types — greyed out (opacity-50) until salary is entered, become coloured gradient cards once salary > 0. Savings Projection and Expense Breakdown charts remain in the right column but only render after Calculate is clicked (using `results` from API). Collapses to single column on mobile (form first, stats below). The form card header "Son Of Mervan" retained for test compatibility.
- `web/src/tests/SonOfMervan.test.jsx`: Updated two tests — "no results shown initially" replaced with "live stat cards shown initially; charts not shown until after Calculate"; "after Calculate shows result cards" now asserts chart headings appear (not the always-visible stat card labels).

**Tests:** 51 frontend tests pass (16 in SonOfMervan suite); build 320 kB gzip; commit: `c4da404`

## Phase 14.3 — Global Visual Polish [DONE] 2026-04-08

**Problem solved:** 18 page-level components each had their own ad-hoc card styles, max-widths, spacing, and input classNames added independently across phases. No consistent visual language.

**What was built:**
- `web/src/components/Card.jsx`: Reusable card primitive — `rounded-2xl shadow-sm border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-5 sm:p-7`. Accepts `className` for overrides.
- `web/src/components/PageWrapper.jsx`: Page-level centering wrapper — `max-w-6xl mx-auto space-y-6`. Accepts optional `className` for pages needing narrower max-width.
- `web/src/styles/inputs.js`: Shared `INPUT_CLASS` and `INPUT_CLASS_INLINE` constants — `border border-gray-300 dark:border-gray-600 rounded-lg px-3 py-2 text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 min-h-[44px] focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent`.
- `web/src/components/EmptyState.jsx`: Empty-state component — icon + heading + description + CTA button, centred with `py-16`.
- All 18 page-level components updated: `PageWrapper` wraps the top-level return, inline card divs replaced with `<Card>`, input classNames replaced with `INPUT_CLASS`/`INPUT_CLASS_INLINE`.
- `SavingsGoalsPage.jsx`: Added `<EmptyState>` when `goals.length === 0 && !showCreate`.
- `NetWorthPage.jsx`: Added `<EmptyState>` when `snapshots.length === 0 && !showForm`. Fixed undefined `input-base` and `btn-primary` CSS classes (replaced with Tailwind equivalents).
- `ScenarioPlannerPage.jsx`: Fixed mismatched `</div>` → `</Card>` closing tag introduced during the update.

**Tests:** 51 frontend tests pass; build succeeds (no ESLint errors); commit: `af91eab`

## Phase 15.1 — Backend: BankConnection & BankTransaction DB Models [DONE] 2026-04-08

**Problem solved:** No database schema existed for storing OAuth bank connections or imported transaction drafts needed for open banking integration.

**What was built:**
- `database.py`: Added `BankConnection` model — stores TrueLayer OAuth tokens (access, refresh), provider name, account ID, last_synced_at, soft-delete via `disconnected_at`. All PII/token fields Fernet-encrypted via hybrid properties.
- `database.py`: Added `BankTransaction` model — stores imported transactions with `draft/confirmed/rejected` status lifecycle, dedup via `external_id_encrypted`, links to `MonthlyExpense` on confirm. All financial/PII fields encrypted.
- `alembic/versions/u1v2w3x4y5z6_add_banking_tables.py`: Migration creates both tables with indexes; downgrade drops them cleanly.
- `.env.example`: Added `TRUELAYER_CLIENT_ID`, `TRUELAYER_CLIENT_SECRET`, `TRUELAYER_REDIRECT_URI` variables.

**Tests:** 481 backend tests pass (all existing tests unaffected); alembic upgrade/downgrade verified; ruff lint clean; commit: `f3ac819`

## Phase 15.2 — Backend: TrueLayer OAuth Flow [DONE] 2026-04-08

**Problem solved:** No mechanism to authorise TrueLayer open banking access or persist OAuth tokens.

**What was built:**
- `routers/banking.py`: `GET /banking/connect` — generates HMAC-signed CSRF state token + TrueLayer authorisation URL with scopes `accounts transactions balance`; returns `{ auth_url }`.
- `routers/banking.py`: `GET /banking/callback` — validates state, exchanges auth code for access+refresh tokens, fetches first account_id, upserts `BankConnection` row (encrypted tokens), redirects to frontend `/banking?connected=true`.
- `routers/banking.py`: `POST /banking/refresh/{id}` — refreshes access token using stored refresh token; called automatically by sync endpoint on 401.
- `routers/banking.py`: `GET /banking/connections` — lists active (non-disconnected) connections; token fields excluded from response.
- `main.py`: banking router registered.
- `models.py`: `BankConnectResponse`, `BankConnectionResponse`, `BankConnectionListResponse`.
- `tests/test_banking.py`: 23 tests covering all endpoints.

**Tests:** 504 backend tests pass; ruff lint clean; commit: `367dc39`

## Phase 15.3 — Backend: Transaction Sync & Draft Review [DONE] 2026-04-08

**Problem solved:** Once connected, users had no way to fetch transactions or review/confirm them as expenses.

**What was built:**
- `routers/banking.py`: `POST /banking/sync` — fetches transactions from TrueLayer `/data/v1/accounts/{id}/transactions` since `last_synced_at` (or 90-day window on first sync). Deduplicates by `external_id`. Calls inline category-suggestion logic (mirrors `GET /insights/suggest-category`). Creates `BankTransaction` draft rows. Auto-refreshes token on 401. Updates `last_synced_at`. Rate-limited 1/5min via slowapi.
- `routers/banking.py`: `GET /banking/drafts?page&page_size` — paginated list of user's `status=draft` transactions (decrypted fields).
- `routers/banking.py`: `PATCH /banking/drafts/{id}` — `action=confirm` creates a `MonthlyExpense` row (using `body.category` or `suggested_category` or "Other"), sets `status=confirmed` + `monthly_expense_id`; `action=reject` sets `status=rejected`. Returns 403 if draft belongs to another user.
- `routers/banking.py`: `POST /banking/drafts/confirm-all` — bulk confirms all draft transactions for the authenticated user.
- `models.py`: Added `BankSyncResponse`, `BankTransactionResponse`, `BankDraftsResponse`, `BankDraftActionRequest`, `BankDraftActionResponse`, `BankConfirmAllResponse`.
- `tests/test_banking.py`: Extended with 26 new tests covering sync deduplication, pagination, confirm/reject ownership, confirm-all, and edge cases.

**Tests:** 530 backend tests pass (91% coverage); ruff lint clean; commit: `4302a28`

## Phase 15.4 — Backend: Bank Connection Disconnect [DONE] 2026-04-08

**Problem solved:** Users had no way to revoke TrueLayer access or clean up imported banking data.

**What was built:**
- `routers/banking.py`: `DELETE /banking/connections/{id}` — verifies ownership; calls TrueLayer token revocation (`POST {auth_base}/connect/revoke`) best-effort (proceeds on network failure); sets `disconnected_at` on `BankConnection` (soft-delete from connections list); hard-deletes all `BankTransaction` rows in `draft` status for the connection; nulls `bank_connection_id` on `confirmed`/`rejected` rows (preserving confirmed expense data).
- `database.py`: Made `bank_connection_id` nullable on `BankTransaction` to support the FK-null pattern for confirmed rows post-disconnect.
- `alembic/versions/v2w3x4y5z6a7_make_bank_connection_id_nullable.py`: Migration alters column to nullable; downgrade restores NOT NULL.
- `models.py`: Added `BankDisconnectResponse` (id, drafts_deleted, confirmed_preserved).
- `tests/test_banking.py`: 11 new tests covering: disconnected_at set, correct counts returned, draft hard-delete, confirmed FK nulled, connection excluded from list, 404 on missing/other-user/already-disconnected, revocation POST called with correct URL, graceful proceed on revocation network failure, unauthenticated 4xx.

**Tests:** 541 backend tests pass (91% coverage); ruff lint clean; commit: `cd51a0c`

---

## Phase 15.5 — Frontend: Bank Connection Page (2026-04-09)

**Task:** Build the full banking UI — connect, sync, review and disconnect flow.

**Files created:**
- `web/src/api/banking.js` — API wrappers: getConnectUrl, listConnections, syncTransactions, disconnectBank, listDrafts, reviewDraft, confirmAllDrafts
- `web/src/hooks/useBanking.js` — React Query hooks: useConnections, useDrafts, useConnectBank, useSyncTransactions, useDisconnectBank, useReviewDraft, useConfirmAllDrafts
- `web/src/components/BankConnectionPage.jsx` — Full page: disconnected CTA card, connected card (sync/disconnect), drafts table with per-row confirm/reject + bulk confirm-all, pagination, sandbox yellow banner, ConfirmModal for disconnect

**Files modified:**
- `web/src/router.jsx` — Added lazy `/banking` route
- `web/src/components/AuthGuard.jsx` — Added `["/banking", "Banking", Landmark]` to SECONDARY_NAV

**Tests:** 541 backend tests passing (91% coverage), 51 frontend tests passing, ruff clean.
**Status:** DONE — committed and pushed (56bcefd).

## Phase 16.2 — Scheduled Expired Token Cleanup [DONE] 2026-04-09
- `main.py`: Added `PasswordResetToken` to the database import line. Added `purge_expired_tokens(session_factory)` job function — hard-deletes `RefreshToken` rows where `expires_at < now` and `PasswordResetToken` rows where `expires_at < now` OR `used_at IS NOT NULL`. Registered in `_start_scheduler` as a daily cron at 03:00 UTC (`id="purge_expired_tokens"`). Updated startup log message to include the new job.
- `tests/test_token_cleanup.py` (new): 7 tests across `TestPurgeExpiredTokens` — expired refresh deleted, live refresh kept, expired reset deleted, used reset deleted, live reset kept, mixed-batch (all combinations verified in one pass), noop when no tokens exist. Uses `session_factory` fixture pattern (MagicMock wrapping `db`, `db.close` mocked) consistent with `test_digest.py`.
- 553 backend tests pass; 91.45% coverage; ruff clean.

## Phase 16.3 — iCal Export of Recurring Expenses [DONE] 2026-04-09

**Task:** Export recurring expense schedule as a `.ics` file importable into Google/Apple Calendar.

**Files modified:**
- `routers/export.py`: Added `_next_occurrence()` helper (computes first occurrence >= today per frequency), `_build_ical()` helper (generates RFC 5545 VCALENDAR text with one VEVENT+RRULE per active expense; UNTIL set when end_date present), and `GET /export/calendar.ics` endpoint (auth required, returns `text/calendar`, `Content-Disposition: attachment; filename="recurring-expenses.ics"`).
- `web/src/api/export.js`: Added `exportCalendar()` function using blob download helper.
- `web/src/components/RecurringExpensesPage.jsx`: Added "Export to Calendar" button (Calendar icon from lucide-react) with loading state, placed before the existing "Generate now" button.
- `tests/test_export.py`: Added `make_recurring()` helper and `TestCalendarExport` class — 6 tests: empty calendar valid, content-disposition header, VEVENT with expense data, all 4 RRULE frequencies (MONTHLY/WEEKLY/DAILY/YEARLY), unauthenticated 401/403, soft-deleted expenses excluded.

**Tests:** 559 backend tests pass; ruff clean; commit: `7352a0a`.

## Phase 13.5 — Active Session Manager [DONE] (retroactive mark) 2026-04-09

**Task:** PRD 13.5 was already fully implemented but not marked DONE. Marked DONE retroactively after confirming all backend (routers/signup.py sessions endpoints, migration), frontend (AccountSettings.jsx Active Sessions card, web/src/api/sessions.js, hooks), and tests (tests/test_sessions.py) were complete.

## Phase 16.4 — Spending Budget Carry-Forward Card [DONE] 2026-04-09

**Task:** Surface unspent budget at month-end so users can move the surplus to savings.

**Files modified:**
- `routers/insights.py`: Added `GET /insights/month-close-summary?month=YYYY-MM` endpoint. Returns `{ month, total_unspent, categories: [{category, planned, actual, unspent}] }` sorted by unspent descending. Overspend categories clamped to `unspent=0`. Returns empty list when no data exists for the month.
- `web/src/api/insights.js`: Added `getMonthCloseSummary(month)` function.
- `web/src/hooks/useInsights.js`: Added `useMonthCloseSummary(month)` React Query hook (staleTime 2 min).
- `web/src/components/SonOfMervan.jsx`: Added Month Close card visible from day 22 of the current month when `total_unspent > 0`. Shows formatted surplus amount and a "Move to savings" button that navigates to `/savings`. Uses `Wallet` icon from lucide-react.
- `tests/test_insights.py`: Added `TestMonthCloseSummary` class — 7 tests: empty data, unspent computed correctly, overspend clamped to zero, sorted descending, data isolation, unauthenticated 401, invalid month format 422.

**Tests:** 566 backend tests pass; 91.6% coverage; ruff clean; commit: `b04cedf`.
