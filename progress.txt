## Phase 2.7 — Backend Test Suite [DONE] 2026-04-06
- Created `tests/conftest.py`: sets env vars before import, patches alembic migrations to skip at test startup, creates SQLite test DB with `Base.metadata.create_all`, autouse fixture truncates all tables + resets rate-limiter storage between tests; fixtures: `db`, `verified_user`, `second_user`, `client` (unauthenticated), `auth_client` (overrides `verify_token` + `get_db`); helper functions `make_month` and `make_expense`
- Created `tests/test_auth.py`: 48 tests covering root health, signup (success + 5 invalid cases), email verification, login (success + wrong password + unverified + soft-deleted + nonexistent), password reset flow (request always-200, confirm success/invalid/expired/used, invalidates old tokens), refresh token (valid/revoked/expired), logout, account management (get/update profile, change password, delete account)
- Created `tests/test_expenses.py`: 22 tests covering PUT /expenses/{id} (name/category/amounts/wrong-user/404/soft-deleted), DELETE /expenses/{id} (success/soft-delete-db/wrong-user/already-deleted/404), GET /monthly-tracker (empty/all expenses/soft-deleted exclusion/category filter/pagination/salary totals/id field), POST /monthly-tracker (new actuals/update existing/preserve planned/remaining calculation)
- Created `tests/test_budget.py`: 17 tests covering POST /calculate-budget (read-only/no-save/commit-saves-month/saves-expenses/upsert/overspend-rec/surplus-rec/savings-rate/category-breakdown/invalid-month/zero-salary), GET /overview/annual (empty/with-data/totals-aggregate/year-filter/no-param/user-isolation)
- Created `tests/test_encryption.py`: 13 tests covering encrypt/decrypt helpers (roundtrip/none/non-deterministic/missing-key), username field not plaintext, monthly data salary not plaintext, expense name/category/amounts not plaintext, bcrypt password hash
- Fixed pre-existing bug: added `@username.expression` to `User.username` hybrid property in `database.py` so class-level SQL filters don't raise `AttributeError` (Fernet-encrypted username can't be matched in SQL, but at least it doesn't crash)
- Added `pytest>=7.4.0`, `pytest-cov>=4.1.0`, `httpx>=0.24.0` to `requirements.txt`; created `pytest.ini`
- Result: 94 tests passing, 87% line coverage (target: 80%)
- Files changed: tests/conftest.py (new), tests/test_auth.py (new), tests/test_expenses.py (new), tests/test_budget.py (new), tests/test_encryption.py (new), pytest.ini (new), requirements.txt, database.py, PRD.md

## Phase 2.6 — Account Management [DONE] 2026-04-06
- Added `deleted_at` (nullable DateTime) column to `User` in database.py for 30-day soft-delete
- Created Alembic migration f6a7b8c9d0e1: adds `deleted_at` column + index on `users` table
- Created `routers/users.py` with four endpoints: `GET /users/me` (profile), `PUT /users/me` (update username), `PUT /users/me/password` (change password, requires current password), `DELETE /users/me` (soft-delete + revoke all refresh tokens)
- Registered `users_router` in main.py
- Updated `security.py` `verify_token` to reject soft-deleted users (HTTP 401)
- Updated `POST /login` in main.py to reject soft-deleted users (HTTP 403)
- Added `send_account_deletion_email()` to email_utils.py (SendGrid with console fallback in dev)
- `DELETE /users/me` revokes all active refresh tokens immediately, then sends deletion confirmation email in background
- Created `web/src/components/AccountSettings.jsx`: three sections — Profile (username edit, email read-only), Security (change password with policy validation), Danger Zone (type "DELETE" to confirm account deletion; auto-logout after deletion)
- Updated `web/src/App.js`: imported `AccountSettings`; added "Settings" tab to desktop nav + mobile select; renders `AccountSettings` with `token` and `onLogout` props
- Files changed: database.py, alembic/versions/f6a7b8c9d0e1_add_deleted_at_to_users.py (new), routers/users.py (new), main.py, security.py, email_utils.py, web/src/components/AccountSettings.jsx (new), web/src/App.js

## Phase 2.5 — Pagination and Filtering [DONE] 2026-04-06
- Added `PaginatedExpenseResponse` Pydantic schema to models.py: `{ items, total, page, pages, page_size }`
- Updated `GET /monthly-tracker/{month}` in main.py: added `category` (optional), `page` (default 1), `page_size` (default 25, max 100) query params; decrypts all expenses then filters/paginates in Python (required because fields are Fernet-encrypted); `expenses` field now returns paginated envelope instead of flat array; `rows` field preserved for backward compat
- Created Alembic migration e5f6a7b8c9d0: composite index `(monthly_data_id, deleted_at)` + standalone index `(deleted_at)` on `monthly_expenses` table
- Updated `web/src/components/MonthlyTracker.jsx`: added `filterCategory`, `currentPage`, `pagination` state; extracted `buildRowsFromExpenses()` helper; data fetch passes `page`, `page_size`, and optional `category` params; skips localStorage cache when filter is active; added filter bar with category dropdown and "Clear filter" button; added prev/next pagination controls (shown only when pages > 1); page resets to 1 on month or filter change
- Files changed: models.py, main.py, alembic/versions/e5f6a7b8c9d0_add_expense_indexes.py (new), web/src/components/MonthlyTracker.jsx

## Phase 2.4 — JWT Refresh Token Mechanism [DONE] 2026-04-06
- Added `RefreshToken` model to database.py: stores `token_hash` (SHA-256), `user_id`, `expires_at`, `revoked_at`, `created_at`; raw token never persisted
- Created Alembic migration d4e5f6a7b8c9 to add `refresh_tokens` table with indexes
- Added `REFRESH_TOKEN_TTL_DAYS: int = 30` to core/config.py
- Updated `POST /login` in main.py: access token TTL reduced 24h → 15 min; generates raw refresh token, stores SHA-256 hash, sets httpOnly cookie (`secure` in prod, `samesite=lax`, 30-day max-age)
- Added `POST /auth/refresh` to routers/signup.py: reads `refresh_token` cookie, validates hash + expiry + not revoked, issues new 15-min access token
- Added `POST /auth/logout` to routers/signup.py: revokes refresh token in DB, deletes cookie
- Updated `web/src/App.js`: removed localStorage token storage; on startup calls `POST /auth/refresh` with `credentials: "include"` to restore session from cookie; `handleLogin` stores token in React state only; `handleLogout` calls `/auth/logout` before clearing state
- 401 auto-retry Axios interceptor deferred to Phase 3.1 (centralized API client)
- Files changed: database.py, alembic/versions/d4e5f6a7b8c9_add_refresh_tokens.py (new), core/config.py, main.py, routers/signup.py, web/src/App.js

## Phase 2.3 — Password Reset Flow [DONE] 2026-04-06
- Added `PasswordResetToken` model to database.py: stores `token_hash` (SHA-256), `user_id`, `expires_at`, `used_at`; raw token never persisted
- Created Alembic migration c3d4e5f6a7b8 to add `password_reset_tokens` table with indexes
- Added `PASSWORD_RESET_TTL_MIN: int = 60` to core/config.py
- Added `send_password_reset_email()` to email_utils.py (SendGrid with console fallback in dev)
- Added `POST /auth/password-reset-request`: always returns success (prevents user enumeration), invalidates prior unused tokens, generates `secrets.token_urlsafe(32)` raw token, stores SHA-256 hash, sends email/dev link
- Added `POST /auth/password-reset-confirm`: hashes incoming token, validates against DB (404 if missing, 400 if used/expired), updates `password_hash`, marks token `used_at`
- Both endpoints enforce same password policy (uppercase, lowercase, digit, special char)
- Created `web/src/components/ForgotPasswordPage.jsx`: email form, shows dev link in non-prod
- Created `web/src/components/ResetPasswordPage.jsx`: reads token from hash query string, new+confirm password fields, success state shows "Go to login" button
- Updated `web/src/components/LoginPage.jsx`: added `goToForgotPassword` prop + "Forgot password?" link below form
- Updated `web/src/App.js`: added `#/reset-password` and `forgot-password` authMode routes; imports and renders new pages
- Files changed: database.py, alembic/versions/c3d4e5f6a7b8_add_password_reset_tokens.py (new), core/config.py, email_utils.py, routers/signup.py, web/src/components/ForgotPasswordPage.jsx (new), web/src/components/ResetPasswordPage.jsx (new), web/src/components/LoginPage.jsx, web/src/App.js

## Phase 2.2 — Full CRUD for Expenses and Income [DONE] 2026-04-06
- Added `deleted_at` (nullable DateTime) column to `MonthlyExpense` in database.py for soft-delete
- Created Alembic migration b2e4f5a6c7d8 to add `deleted_at`; upgrade/downgrade verified against SQLite
- Added `ExpenseUpdateRequest` and `ExpenseResponse` Pydantic schemas to models.py
- Added `PUT /expenses/{id}` endpoint: updates name/category/planned_amount/actual_amount, validates ownership (403 if wrong user)
- Added `DELETE /expenses/{id}` endpoint: soft-delete (sets `deleted_at`), returns 204, validates ownership
- Updated all existing expense queries in `calculate-budget`, `save_actuals`, and `get_monthly_tracker` to filter `deleted_at == None`
- Updated `GET /monthly-tracker/{month}` to return an `expenses` array with individual rows including DB `id` fields
- Created `web/src/components/ConfirmModal.jsx`: generic confirmation dialog with Cancel/Confirm buttons
- Updated `web/src/components/MonthlyTracker.jsx`:
  - Loads individual expenses from server `expenses` array; stores DB id per row
  - Pencil icon on saved rows triggers inline edit mode; Check/X icons to save/cancel
  - Inline save calls `PUT /expenses/{id}`; trash on saved rows opens ConfirmModal → `DELETE /expenses/{id}`
  - After batch save, re-fetches server data so new DB ids populate rows
- Files changed: database.py, alembic/versions/b2e4f5a6c7d8_add_deleted_at_to_monthly_expenses.py (new), models.py, main.py, web/src/components/ConfirmModal.jsx (new), web/src/components/MonthlyTracker.jsx

## Phase 2.1 — Alembic Database Migrations [DONE] 2026-04-06
- Added alembic>=1.13.0 and pinned sqlalchemy>=2.0.0 to requirements.txt
- Ran `alembic init alembic` to create scaffold (alembic/ dir + alembic.ini)
- Configured alembic/env.py: imports Base and engine from database.py, overrides sqlalchemy.url from DATABASE_URL env var, reuses app engine so PostgreSQL SSL args are inherited
- Generated initial migration (a047a3ff3bf1) via --autogenerate; captures users, monthly_data, monthly_expenses tables with all columns and indexes
- Replaced `init_db()` call in main.py with `alembic.command.upgrade(cfg, "head")` — schema is now migration-managed on every startup
- Verified: `alembic upgrade head` applies cleanly on a fresh DB; `alembic downgrade -1` rolls back; second startup is a no-op (already at head)
- Files changed: requirements.txt, alembic.ini (new), alembic/env.py (new), alembic/versions/a047a3ff3bf1_initial_schema.py (new), main.py

## Phase 1.4 — Security Headers & CORS Hardening [DONE] 2026-04-06
- Created middleware/security.py with SecurityHeadersMiddleware (Starlette BaseHTTPMiddleware)
- Headers added to every response: X-Content-Type-Options, X-Frame-Options, Content-Security-Policy, Referrer-Policy, Permissions-Policy
- HSTS (Strict-Transport-Security) added only when ENVIRONMENT=production (safe for local HTTP dev)
- Replaced wildcard CORS allow_methods=["*"] and allow_headers=["*"] with explicit safe lists
- Added ENVIRONMENT setting to core/config.py (default: "development")
- Documented ENVIRONMENT in .env.example
- Files changed: middleware/security.py (new), middleware/__init__.py (new), core/config.py, main.py, .env.example

## Phase 1.3 — Rate Limiting on Auth Endpoints [DONE] 2026-04-06
- Added slowapi==0.1.9 to requirements.txt
- Created core/limiter.py with shared Limiter instance (in-memory, keyed by remote IP)
- Registered limiter on app.state and RateLimitExceeded exception handler in main.py
- Applied @limiter.limit("5/minute") to POST /login (main.py) and POST /auth/signup (routers/signup.py)
- Both endpoints now accept request: Request as required by slowapi
- Returns HTTP 429 with Retry-After header on the 6th request within 60 seconds
- Files changed: core/limiter.py (new), main.py, routers/signup.py, requirements.txt

## Phase 1.2 — Fix JWT Secret Fallback [DONE] 2026-04-06
- Added core/config.py with Pydantic BaseSettings; JWT_SECRET_KEY is required (no default)
- App now exits immediately with a clear [FATAL] message if JWT_SECRET_KEY is missing
- security.py and main.py updated to import from core.config (no more os.getenv fallbacks)
- Added pydantic-settings>=2.0.0 to requirements.txt
- Created .env.example documenting all required and optional variables
- Files changed: core/config.py (new), security.py, main.py, requirements.txt, .env.example (new)

## Phase 1.1 — Remove debug/migration HTTP endpoints [DONE] 2026-04-06
- Removed POST /run-migration, POST /cleanup-old-columns, GET /debug/check-month/{month} from main.py
- Replaced all print() calls with stdlib logging (LOG_LEVEL env var controlled)
- Added core/logging_config.py and scripts/migrate.py CLI as replacement
- Files changed: main.py, email_utils.py, routers/signup.py, core/, scripts/
